{
  "version": 3,
  "id": "mr1x7j5i-zioba9",
  "objective": "=== Goal ===\n\nReplace the per-repo `daemon-standalone` branch with `main` across the 10 standalone game/hegemon worktrees (`/home/dracon/Dev/<name>/` for `<name>` in `{polis, darklord, neonbreak, capture-anime-girls, hellhunter, junk-runner, endless-td, deathrun, one-mil-girls, hegemon}`), keep them marked as submodules in `dracon-platform`, and keep them as normal standalone repos on github/gitlab/codeberg.\n\n## Background (current state)\n\n- Each of the 10 repos is a standalone repo with 3 remotes (github/gitlab/codeberg) and is declared as a submodule in `dracon-platform/.gitmodules` with `branch = main` (already correct).\n- The daemon also auto-materializes each as a standalone worktree at `/home/dracon/Dev/<name>/`. Today those worktrees are on a per-repo `daemon-standalone` branch (created in goal `mr10pdzr-i495vy` because git forbids two worktrees on the same branch — the nested checkout inside `dracon-platform/web/games/<wip|released>/<name>/` is on `main`).\n- The naming is confusing (looks like a repo) AND the daemon's `shared_submodule_canonical_head_sha` helper prefers `daemon-standalone` over `main`, which causes the parent's gitlink to stay stale when `main` is ahead (e.g. polis, hellhunter, hegemon currently have main ahead of daemon-standalone).\n- The nested checkouts are already on `main` (4 of 10) or detached at a specific SHA (6 of 10). The detached ones are not a problem; the on-`main` ones need to be detached before the standalone can be put on `main`.\n\n## Success criteria\n\n1. **All 10 standalone worktrees are on `main`** (not `daemon-standalone`): `dracon-sync repos` shows `BRANCH = main` and `🔗 PUBLISH = origin/main` for all 10 rows.\n2. **Nested checkouts are not on `main`**: For the 4 currently-on-`main` cases (polis, hellhunter, one-mil-girls, hegemon), the nested checkout at `dracon-platform/web/games/<wip|released>/<name>/` is detached at the parent's current gitlink SHA. (The 6 already-detached cases are unchanged.)\n3. **Parent gitlink matches standalone HEAD** for all 10 submodules (the convergence invariant `standalone HEAD == parent gitlink` holds for each).\n4. **`shared_submodule_canonical_head_sha` no longer mentions `daemon-standalone`**: it returns the shared gitdir's `refs/heads/main` SHA only (or None if missing).\n5. **The daemon no longer creates a `daemon-standalone` branch** when materializing a new standalone worktree: `src/sync.rs:1309` `git worktree add -b daemon-standalone <path> <sha>` becomes `git worktree add <path> <sha>` (no `-b`).\n6. **The \"convert detached → daemon-standalone branch\" code in `src/sync.rs:1198-1213` is removed** (no longer needed since we use the existing `main` branch).\n7. **End-to-end touch test passes on at least 3 of the 10 submodules** (touch a file in the standalone, wait 90s, verify: (1) auto-commit in standalone, (2) auto-push to all 3 remotes, (3) auto-commit in `dracon-platform` updating the gitlink, (4) auto-push to codeberg).\n8. **`cargo test --locked` passes 0 failures, `cargo build --release --locked` succeeds, new binary replaces `/home/dracon/.local/bin/dracon-sync`, daemon is restarted with the new binary.**\n9. **The remote `daemon-standalone` branch is deleted from all 3 remotes (github/gitlab/codeberg) for each of the 10 repos** (or, if deletion is blocked by forge policy, a note is added to the design doc explaining why).\n\n## In-scope\n\n- 10 game/hegemon repos at `/home/dracon/Dev/<name>/` and their nested checkouts inside `dracon-platform`\n- Daemon code in `/home/dracon/Dev/dracon-utilities/dracon-sync/`:\n  - `src/sync.rs` (the worktree creation + the detached→branch converter)\n  - `src/exclude.rs` (`shared_submodule_canonical_head_sha` and any daemon-standalone references)\n  - `src/git/discovery.rs` (any daemon-standalone refs in module discovery)\n  - related unit/integration tests\n- `/home/dracon/.dracon/sync-status.json` (update `branch = main` for all 10 rows)\n- A new design doc at `/home/dracon/Dev/dracon-utilities/docs/design/daemon-standalone-removal-2026-07-01.md` explaining the change\n- `AGENTS.md` in `dracon-utilities/` — update any references to `daemon-standalone`\n\n## Out of scope\n\n- Any non-game repo (e.g. `dracon-utilities`, `dracon-platform`'s own branches, `ai-auto-writer`, `web-auto`, `pully-...`, `hegemon` non-worktree state)\n- Touching the canonical public `main` branch history of any game repo (we never rewrite or rebase public history)\n- The `dracon-platform` parent's own push-stuck state (separate concern, already being worked on)\n- `auto_github_private` / `auto_rewrite_large_blobs` / any other daemon policy\n\n## Hard constraints\n\n- **Never rewrite or rebase public history** of any of the 10 game repos. All `main` updates are fast-forwards only.\n- **Never delete the standalone worktree or the nested checkout**. The daemon must continue to function with both worktrees.\n- **`cargo test --locked` must pass with 0 failures** (don't break the existing 668 tests).\n- **All 10 standalone worktrees must remain clean (MOD=0, UT=0)** after the rename — no leftover untracked files.\n- **The parent's gitlink must end up at the same SHA as the standalone's HEAD** for each of the 10 submodules (convergence invariant).\n- **The existing `.gitmodules` in `dracon-platform` is unchanged** — the submodule definitions (path, urls, `branch = main`) are already correct.\n\n## If blocked\n\n- If a forge (github/gitlab/codeberg) refuses to delete the remote `daemon-standalone` branch, do not force-push or rewrite; just document the residual remote branches in the design doc.\n- If the daemon's tests fail, do not skip them; fix the code or update the test to match the new design (with a comment explaining why).\n- If the parent's push is stuck (it currently is), do not attempt to fix that here; work around it by manual `git push` from the parent as needed, and document the workaround in the design doc.\n\n=== End Goal ===",
  "status": "active",
  "autoContinue": true,
  "usage": {
    "tokensUsed": 1719690,
    "activeSeconds": 3024
  },
  "sisyphus": false,
  "createdAt": "2026-07-01T10:16:18.102Z",
  "updatedAt": "2026-07-01T11:15:53.710Z",
  "activePath": ".pi/goals/active_goal_2026070111161810_mr1x7j5i-zioba9.md"
}

# Goal Prompt

=== Goal ===

Replace the per-repo `daemon-standalone` branch with `main` across the 10 standalone game/hegemon worktrees (`/home/dracon/Dev/<name>/` for `<name>` in `{polis, darklord, neonbreak, capture-anime-girls, hellhunter, junk-runner, endless-td, deathrun, one-mil-girls, hegemon}`), keep them marked as submodules in `dracon-platform`, and keep them as normal standalone repos on github/gitlab/codeberg.

## Background (current state)

- Each of the 10 repos is a standalone repo with 3 remotes (github/gitlab/codeberg) and is declared as a submodule in `dracon-platform/.gitmodules` with `branch = main` (already correct).
- The daemon also auto-materializes each as a standalone worktree at `/home/dracon/Dev/<name>/`. Today those worktrees are on a per-repo `daemon-standalone` branch (created in goal `mr10pdzr-i495vy` because git forbids two worktrees on the same branch — the nested checkout inside `dracon-platform/web/games/<wip|released>/<name>/` is on `main`).
- The naming is confusing (looks like a repo) AND the daemon's `shared_submodule_canonical_head_sha` helper prefers `daemon-standalone` over `main`, which causes the parent's gitlink to stay stale when `main` is ahead (e.g. polis, hellhunter, hegemon currently have main ahead of daemon-standalone).
- The nested checkouts are already on `main` (4 of 10) or detached at a specific SHA (6 of 10). The detached ones are not a problem; the on-`main` ones need to be detached before the standalone can be put on `main`.

## Success criteria

1. **All 10 standalone worktrees are on `main`** (not `daemon-standalone`): `dracon-sync repos` shows `BRANCH = main` and `🔗 PUBLISH = origin/main` for all 10 rows.
2. **Nested checkouts are not on `main`**: For the 4 currently-on-`main` cases (polis, hellhunter, one-mil-girls, hegemon), the nested checkout at `dracon-platform/web/games/<wip|released>/<name>/` is detached at the parent's current gitlink SHA. (The 6 already-detached cases are unchanged.)
3. **Parent gitlink matches standalone HEAD** for all 10 submodules (the convergence invariant `standalone HEAD == parent gitlink` holds for each).
4. **`shared_submodule_canonical_head_sha` no longer mentions `daemon-standalone`**: it returns the shared gitdir's `refs/heads/main` SHA only (or None if missing).
5. **The daemon no longer creates a `daemon-standalone` branch** when materializing a new standalone worktree: `src/sync.rs:1309` `git worktree add -b daemon-standalone <path> <sha>` becomes `git worktree add <path> <sha>` (no `-b`).
6. **The "convert detached → daemon-standalone branch" code in `src/sync.rs:1198-1213` is removed** (no longer needed since we use the existing `main` branch).
7. **End-to-end touch test passes on at least 3 of the 10 submodules** (touch a file in the standalone, wait 90s, verify: (1) auto-commit in standalone, (2) auto-push to all 3 remotes, (3) auto-commit in `dracon-platform` updating the gitlink, (4) auto-push to codeberg).
8. **`cargo test --locked` passes 0 failures, `cargo build --release --locked` succeeds, new binary replaces `/home/dracon/.local/bin/dracon-sync`, daemon is restarted with the new binary.**
9. **The remote `daemon-standalone` branch is deleted from all 3 remotes (github/gitlab/codeberg) for each of the 10 repos** (or, if deletion is blocked by forge policy, a note is added to the design doc explaining why).

## In-scope

- 10 game/hegemon repos at `/home/dracon/Dev/<name>/` and their nested checkouts inside `dracon-platform`
- Daemon code in `/home/dracon/Dev/dracon-utilities/dracon-sync/`:
  - `src/sync.rs` (the worktree creation + the detached→branch converter)
  - `src/exclude.rs` (`shared_submodule_canonical_head_sha` and any daemon-standalone references)
  - `src/git/discovery.rs` (any daemon-standalone refs in module discovery)
  - related unit/integration tests
- `/home/dracon/.dracon/sync-status.json` (update `branch = main` for all 10 rows)
- A new design doc at `/home/dracon/Dev/dracon-utilities/docs/design/daemon-standalone-removal-2026-07-01.md` explaining the change
- `AGENTS.md` in `dracon-utilities/` — update any references to `daemon-standalone`

## Out of scope

- Any non-game repo (e.g. `dracon-utilities`, `dracon-platform`'s own branches, `ai-auto-writer`, `web-auto`, `pully-...`, `hegemon` non-worktree state)
- Touching the canonical public `main` branch history of any game repo (we never rewrite or rebase public history)
- The `dracon-platform` parent's own push-stuck state (separate concern, already being worked on)
- `auto_github_private` / `auto_rewrite_large_blobs` / any other daemon policy

## Hard constraints

- **Never rewrite or rebase public history** of any of the 10 game repos. All `main` updates are fast-forwards only.
- **Never delete the standalone worktree or the nested checkout**. The daemon must continue to function with both worktrees.
- **`cargo test --locked` must pass with 0 failures** (don't break the existing 668 tests).
- **All 10 standalone worktrees must remain clean (MOD=0, UT=0)** after the rename — no leftover untracked files.
- **The parent's gitlink must end up at the same SHA as the standalone's HEAD** for each of the 10 submodules (convergence invariant).
- **The existing `.gitmodules` in `dracon-platform` is unchanged** — the submodule definitions (path, urls, `branch = main`) are already correct.

## If blocked

- If a forge (github/gitlab/codeberg) refuses to delete the remote `daemon-standalone` branch, do not force-push or rewrite; just document the residual remote branches in the design doc.
- If the daemon's tests fail, do not skip them; fix the code or update the test to match the new design (with a comment explaining why).
- If the parent's push is stuck (it currently is), do not attempt to fix that here; work around it by manual `git push` from the parent as needed, and document the workaround in the design doc.

=== End Goal ===

## Progress

- Status: running
- Auto-continue: on
- Sisyphus mode: no
- Time spent: 50m24s
- Tokens used: 1.7M (1,719,690) tokens
