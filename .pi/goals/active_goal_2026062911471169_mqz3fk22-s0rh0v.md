{
  "version": 3,
  "id": "mqz3fk22-s0rh0v",
  "objective": "Fix the remaining ugly rendering in `dracon-sync repos` at 250+ cols: (1) unowned STATE cells wrap mid-content, (2) PENDING PUSH cells show icon+label on separate lines, (3) column headers like STATUS wrap to STA/TUS. Root cause: previous constraints use `LowerBoundary` which can EXCEED available width when total minimums > terminal width — comfy-table resolves this by wrapping content. Fix by either computing total-min vs available and switching tier boundaries (or capping minimums), OR by changing the full layout to absolute widths without LowerBoundary, OR by ensuring ALL minimums sum to < terminal width at 200 cols.",
  "status": "active",
  "autoContinue": true,
  "usage": {
    "tokensUsed": 196730,
    "activeSeconds": 232
  },
  "sisyphus": false,
  "createdAt": "2026-06-29T10:47:11.690Z",
  "updatedAt": "2026-06-29T10:51:24.939Z",
  "activePath": ".pi/goals/active_goal_2026062911471169_mqz3fk22-s0rh0v.md",
  "taskList": {
    "tasks": [
      {
        "id": "audit-22col-full-render",
        "title": "Audit current 250-col rendering: capture sample at exact 200, 250, 300 col widths",
        "status": "complete",
        "completedAt": "2026-06-29T10:47:52.727Z",
        "evidence": "Captured output at 200/250/300 cols. All three produce identical table (comfy-table always uses minimums at 200+, distributing surplus). Header row 5 confirms: '🏷 STA / TUS', '🌿 BRANCH' wraps to 2 l",
        "verificationContract": "Run ./target/release/dracon-sync repos at DRACON_SYNC_TERM_WIDTH 200/250/300 and capture output. For each width, count which cells wrap, which column headers wrap, and the maximum row height. Confirm unowned/PENDING specifically."
      },
      {
        "id": "compute-min-totals",
        "title": "Compute total LowerBoundary + Absolute minimums and compare to terminal widths",
        "status": "complete",
        "completedAt": "2026-06-29T10:47:58.621Z",
        "evidence": "Sum of 22 column minimums = 206. Plus 23 border characters = 229 cols minimum. But Full tier triggers at 200 cols, so for 200-228 cols the table is constrained to overflow or wrap. 220-229: tight. 230",
        "verificationContract": "Sum the current minimums in print_repos_full_table set_constraints. Calculate the minimum table width (sum + 23 borders). Compare to 200 (compact tier threshold) and 220 (where the previous doc said the full tier should start). Document whether the tier boundary at 200 cols is reachable."
      },
      {
        "id": "rebalance-constraints",
        "title": "Rebalance column constraints: cap each minimum at a sane value and switch to absolute widths",
        "status": "pending",
        "verificationContract": "Replace LowerBoundary constraints with absolute widths that sum to <= 200 cols (so the table fits comfortably at 220+). Keep information density. Verify no cell wraps mid-content. Column headers should not wrap (single-line headers)."
      },
      {
        "id": "fix-push-cell-wrapping",
        "title": "Fix PUSH cell wrapping: PENDING icon+label must be on one line",
        "status": "pending",
        "verificationContract": "Increase PUSH column width to fit '🟣 PENDING' (10 cols) without wrap. Verify all PUSH cells render single-line. If push_text is wider than column, shorten label (e.g., '🟣 PEND' or just emoji)."
      },
      {
        "id": "fix-unowned-cell-wrapping",
        "title": "Fix unowned STATE cell: '🚫 unowned: HEAD author = Audit Bot <audit@noreply.…' must not wrap mid-word",
        "status": "pending",
        "verificationContract": "Truncate the detail more aggressively (e.g., 15 chars instead of 40) and/or widen STATE column. Goal: unowned cell renders single-line as '🚫 unowned: HEAD aut…' or '🚫 unowned: untruste…' without internal break."
      },
      {
        "id": "fix-push-to-cell-wrap",
        "title": "Fix PUSH-TO cell: 'codeberg [excl:github,gitlab]' was wrapping to 'codeberg [exc/l:github,gitl/ab]'",
        "status": "pending",
        "verificationContract": "Widen PUSH-TO column so the full label fits without internal wrap. Or shorten by abbreviating 'excl' to '−' or similar. Verify single-line render for typical 3-remote + 2-excluded configurations."
      },
      {
        "id": "fix-column-header-wrap",
        "title": "Fix column headers: 'STATUS' must render as '🏷 STATUS' single-line, not '🏷 STA/TUS'",
        "status": "pending",
        "verificationContract": "Either: (a) make headers narrower (drop icon, use 4-char abbrev like 'STAT'), (b) increase column width enough for '🏷 STATUS' (~9 cols including icon), or (c) use UpperBoundary to prevent wrap. Verify all 22 column headers render single-line."
      },
      {
        "id": "add-wrap-detection-test",
        "title": "Add unit test that no cell wraps mid-content at the tier boundaries",
        "status": "pending",
        "verificationContract": "Test function renders all repos at 200, 250, 300 cols and asserts: (1) no cell contains a multi-line break inside its content (header-wrap detection: split content by newlines and check each chunk < column width), (2) every column header is on one line, (3) sum of column widths fits within terminal width."
      },
      {
        "id": "build-deploy-push",
        "title": "Build release, restart daemon, push commits",
        "status": "pending",
        "verificationContract": "cargo build --release --locked passes. All existing 617 tests + new wrap-detection test pass. Captured evidence files in docs/design/header-content-wrap-evidence/. Commits pushed to codeberg + gitlab at 0/0. Daemon restarted with new binary SHA256."
      }
    ],
    "blockCompletion": false,
    "proposedAt": "2026-06-29T10:47:11.697Z"
  }
}

# Goal Prompt

Fix the remaining ugly rendering in `dracon-sync repos` at 250+ cols: (1) unowned STATE cells wrap mid-content, (2) PENDING PUSH cells show icon+label on separate lines, (3) column headers like STATUS wrap to STA/TUS. Root cause: previous constraints use `LowerBoundary` which can EXCEED available width when total minimums > terminal width — comfy-table resolves this by wrapping content. Fix by either computing total-min vs available and switching tier boundaries (or capping minimums), OR by changing the full layout to absolute widths without LowerBoundary, OR by ensuring ALL minimums sum to < terminal width at 200 cols.

## Progress

- Status: running
- Auto-continue: on
- Sisyphus mode: no
- Time spent: 3m52s
- Tokens used: 197K (196,730) tokens
## Tasks

<!-- blockCompletion: false -->
- [x] audit-22col-full-render: Audit current 250-col rendering: capture sample at exact 200, 250, 300 col widths — evidence: Captured output at 200/250/300 cols. All three produce identical table (comfy-table always uses minimums at 200+, distributing surplus). Header row 5 confirms: '🏷 STA / TUS', '🌿 BRANCH' wraps to 2 l
- [x] compute-min-totals: Compute total LowerBoundary + Absolute minimums and compare to terminal widths — evidence: Sum of 22 column minimums = 206. Plus 23 border characters = 229 cols minimum. But Full tier triggers at 200 cols, so for 200-228 cols the table is constrained to overflow or wrap. 220-229: tight. 230
- [ ] rebalance-constraints: Rebalance column constraints: cap each minimum at a sane value and switch to absolute widths — contract: Replace LowerBoundary constraints with absolute widths that sum to <= 200 cols (so the table fits comfortably at 220+). Keep information density. Verify no cell wraps mid-content. Column headers should not wrap (single-line headers).
- [ ] fix-push-cell-wrapping: Fix PUSH cell wrapping: PENDING icon+label must be on one line — contract: Increase PUSH column width to fit '🟣 PENDING' (10 cols) without wrap. Verify all PUSH cells render single-line. If push_text is wider than column, shorten label (e.g., '🟣 PEND' or just emoji).
- [ ] fix-unowned-cell-wrapping: Fix unowned STATE cell: '🚫 unowned: HEAD author = Audit Bot <audit@noreply.…' must not wrap mid-word — contract: Truncate the detail more aggressively (e.g., 15 chars instead of 40) and/or widen STATE column. Goal: unowned cell renders single-line as '🚫 unowned: HEAD aut…' or '🚫 unowned: untruste…' without internal break.
- [ ] fix-push-to-cell-wrap: Fix PUSH-TO cell: 'codeberg [excl:github,gitlab]' was wrapping to 'codeberg [exc/l:github,gitl/ab]' — contract: Widen PUSH-TO column so the full label fits without internal wrap. Or shorten by abbreviating 'excl' to '−' or similar. Verify single-line render for typical 3-remote + 2-excluded configurations.
- [ ] fix-column-header-wrap: Fix column headers: 'STATUS' must render as '🏷 STATUS' single-line, not '🏷 STA/TUS' — contract: Either: (a) make headers narrower (drop icon, use 4-char abbrev like 'STAT'), (b) increase column width enough for '🏷 STATUS' (~9 cols including icon), or (c) use UpperBoundary to prevent wrap. Verify all 22 column headers render single-line.
- [ ] add-wrap-detection-test: Add unit test that no cell wraps mid-content at the tier boundaries — contract: Test function renders all repos at 200, 250, 300 cols and asserts: (1) no cell contains a multi-line break inside its content (header-wrap detection: split content by newlines and check each chunk < column width), (2) every column header is on one line, (3) sum of column widths fits within terminal width.
- [ ] build-deploy-push: Build release, restart daemon, push commits — contract: cargo build --release --locked passes. All existing 617 tests + new wrap-detection test pass. Captured evidence files in docs/design/header-content-wrap-evidence/. Commits pushed to codeberg + gitlab at 0/0. Daemon restarted with new binary SHA256.

