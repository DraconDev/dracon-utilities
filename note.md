- we want want version bumps for git sync if we can, no empty cargo lock commits, cause we had such a bug in the past, every version bump is only a patch

- we have the ai lib already lets try to use it via dracon ai 

- dracon code 
  - make sure the context of the convo is always saved in the 