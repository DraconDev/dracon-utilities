[package]
name = "dracon-warden"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
notify = "6.1"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
walkdir = "2.5"
dirs = "6.0"
dracon-security-kit = { path = "../dracon-libs/tools/crates/dracon-security-kit" }
