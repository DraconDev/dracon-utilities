# Goal

**Status**: complete
**Policy**: list
**Auto-continue**: on
**File**: `20260810220741-nx6igz.md`
**Stop reason**: auditor opencode-go/deepseek-v4-flash approved

## Objective

> FIX LOW (sync): SIGHUP reload + freeze marker latency — polled only at loop top, up to one pulse_interval_secs (120s+) delay (daemon.rs:3524-3576)

## Completion summary

Fixed the SIGHUP/freeze-marker latency in the dracon-sync daemon loop: the blind bottom sleep(scan_interval) (up to 120s+) and the post-discovery-only freeze check meant kill -HUP, pause, and resume could take a full pulse plus the cycle body to land. New async helper sleep_responsive (daemon.rs) sleeps in 1s slices and wakes early on (a) a new tokio::sync::Notify wake channel fired by the SIGHUP handler right after setting the reload flag, and (b) freeze-marker flips. Applied at all three sleep sites: a new loop-top freeze check (after scan_interval computation, so pause/resume land within ~1s of the cycle ending), the existing mid-body freeze sleep (now resumes immediately when the marker is removed), and the bottom-of-cycle sleep (wakes on SIGHUP and on a fresh pause marker). Reload flag stays the source of truth (lost notifications are harmless — the flag is seen at loop top). Three new #[tokio::test] tests: notify wake (<2s from a 5s sleep), predicate wake, and full-duration sleep (no early return). All AGENTS.md gates green: workspace tests 24/24 suites ok (949 sync tests), clippy -D warnings clean, deny clean, release build ok. CHANGELOG entry added. Committed d337531 (daemon auto-commit), verified on all three mirrors (github origin, codeberg, gitlab main at d337531). Ledger entry marked fixed.

## Audit history

- 2026-08-10T22:16:45.411Z — approved — `opencode-go/deepseek-v4-flash`
