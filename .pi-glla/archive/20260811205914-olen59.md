# Goal

**Status**: complete
**Policy**: list
**Auto-continue**: on
**File**: `20260811205914-olen59.md`
**Stop reason**: auditor openai-codex/gpt-5.6-luna approved

## Objective

> Fix audit finding: HIGH: `dracon-system/scripts/release.sh:33` — `--help` still advertises `--remote` default `github` while the code default is `origin` (line ~62, CHANGED 2026-08-10 v0.112.35) — the one place an operator checks before a release gives the pre-fix, broken advice —

## Completion summary

The dracon-system release help-text finding is fixed and verified. `scripts/release.sh --help` now correctly advertises `--remote <name>` with default `origin`, matching the code and repository's GitHub remote; the stale `(default: github)` advice is absent.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").

## Audit history

- 2026-08-11T21:02:48.288Z — approved — `openai-codex/gpt-5.6-luna`
