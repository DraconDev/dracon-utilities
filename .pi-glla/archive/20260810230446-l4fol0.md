# Goal

**Status**: complete
**Policy**: list
**Auto-continue**: on
**File**: `20260810230446-l4fol0.md`
**Stop reason**: auditor opencode-go/deepseek-v4-flash approved

## Objective

> FIX MEDIUM (warden): V1 fallback CFB unauthenticated — wrong-key decrypt passing ASCII heuristic returns silent garbage; leaks 20 ciphertext bytes to stderr (crypto.rs:184-201)

## Completion summary

FIX MEDIUM (warden) shipped: the V1 fallback CFB decrypt (decrypt_git_seal, crypto.rs) is no longer a silent-garbage factory, and the ciphertext leak is closed. The old code decrypted AES-256-CFB (key/IV = sha256(repo_key) slices) with NO authentication and filtered on 'first 20 bytes ASCII' only — a wrong-key decrypt whose output merely started printable was returned as garbage plaintext (whole-file secret corruption). Fix (dracon-warden, commits 5ebb515 + dd2a997, all mirrors at HEAD dd2a997): the plausibility gate is now whole-buffer valid UTF-8 AND first-20 ASCII — legacy git-seal payloads are text secrets, so ASCII-prefix + garbage-tail fails even with the correct key, and a wrong-key 300-byte output passes UTF-8 with probability ~1e-60; binary legacy payloads now get a clear error instead of silent corruption. The unlock_payload error path no longer dumps the first 20 CIPHERTEXT bytes to stderr ('Magic: {:?}' removed) — it reports only age-format + length. Three new tests: correct-key CFB roundtrip + wrong-key rejection (deterministic 300-byte payload), the exact hole (20 ASCII bytes + invalid-UTF-8 tail must be rejected even with the correct key, via a legacy_cfb_encrypt helper mirroring the format), and no-ciphertext-dump assertion (error contains len= but no input bytes, no 'Magic:'). Gates green: dracon-security 51 lib tests + all integration suites, warden bin 101 tests, workspace --locked release build, clippy -D warnings clean. Installed ~/.local/bin/dracon-warden refreshed from the release build. Ledger line flipped [x] — fixed in dd2a997; parent repo (ledger) synced to all 3 mirrors.

## Audit history

- 2026-08-10T23:11:48.466Z — approved — `opencode-go/deepseek-v4-flash`
