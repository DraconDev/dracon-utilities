# Goal

**Status**: complete
**Policy**: list
**Auto-continue**: on
**File**: `20260813170617-8eyhdw.md`
**Archive**: `20260813170617-8eyhdw.md`
**Stop reason**: auditor openai-codex/gpt-5.6-luna approved

## Objective

> Fix audit finding: MEDIUM: global pre-push/pre-rebase hooks shadow repo-local hooks with NO chaining — the H-10 fix covers only pre-commit (main.rs:2461-2464); husky/foreign pre-push+pre-rebase hooks are silently disabled fleet-wide under core.hooksPath (main.rs:2506-2661, 2665-2698); `install_hooks_for_repo` seeds (2805-2848) are dead when global hooksPath is set and swallow errors (`let _ =` main.rs:1213) —

## Completion summary

Addressed the auditor's blocking gap in the global hook-chaining fix. `install_hooks_for_repo` now resolves Git's effective hooks directory with `git rev-parse --git-path hooks` and skips seeding `.git/hooks` whenever global or repository-local `core.hooksPath` makes that directory inactive; pre-existing foreign local hooks remain available for the global wrappers to chain. `harden_repo` now propagates hook-installation errors instead of discarding them. Existing pre-push/pre-rebase chaining remains intact.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").

## Audit history

- 2026-08-13T17:28:59.108Z — disapproved — `openai-codex/gpt-5.6-luna`
- 2026-08-13T19:37:16.203Z — approved — `openai-codex/gpt-5.6-luna`
