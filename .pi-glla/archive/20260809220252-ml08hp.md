# Goal

**Status**: complete
**Policy**: list
**Auto-continue**: on
**File**: `20260809220252-ml08hp.md`
**Stop reason**: auditor opencode-go/deepseek-v4-flash approved

## Objective

> Fix audit finding: HIGH: warden's release.sh defaults `REMOTE=github` but the warden repo's remotes are `origin`/`codeberg`/`gitlab` (no `github` — verified `git remote -v`); step 6's `git push "$REMOTE" ...` fails out of the box unless `--remote origin` is remembered. dracon-sync got resolve-github-remote.sh (v0.113.11); warden never did (dracon-warden/scripts/release.sh:59) —

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").

## Audit history

- 2026-08-09T22:07:51.674Z — approved — `opencode-go/deepseek-v4-flash`
