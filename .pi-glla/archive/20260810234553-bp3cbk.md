# Goal

**Status**: complete
**Policy**: list
**Auto-continue**: on
**File**: `20260810234553-bp3cbk.md`
**Stop reason**: auditor opencode-go/deepseek-v4-flash approved

## Objective

> FIX LOW (warden): pre-commit second filter check is dead code — git config without --local reads global (main.rs:2491-2495)

## Completion summary

FIX LOW (warden) shipped: the pre-commit hook's second filter check is no longer dead code. The check at main.rs:2491-2495 (now 2497-2504) read `git config filter.dracon.clean` WITHOUT --local — git config reads local→global→system scopes, and the operator's ~/.gitconfig carries filter.dracon.* (verified live: `git config --show-origin` shows file:/home/dracon/.gitconfig), so the check passed in EVERY repo once MANAGED=1 was set, masking local-config drift in managed repos; only the .gitattributes grep was a real gate. The check now uses `git config --local filter.dracon.clean` — the exact scope `once` writes (ensure_repo_filter_config runs `git config --local`, main.rs:993-1010) — with the error message updated to 'not configured in local git config' and a comment explaining the global-masking hazard (mirroring the MANAGED probe's own 2026-07-26 note at main.rs:2475-2479). New test `pre_commit_hook_blocks_managed_repo_with_only_global_filter_config` proves: a managed repo (.dracon + .gitattributes block) with ONLY global config set (via GIT_CONFIG_GLOBAL pointing at a fixture gitconfig) is now BLOCKED with the 'local git config' message, and the same repo passes once the local key is set (control). Committed as d504ab20b64f6f3ad6106d964c635421d2347cb0 (main.rs + tests.rs DELTA:+65/-3); bin tests 107 passed (106 + 1 new) — full suite now 110 with later items; clippy -p dracon-warden --all-targets -D warnings clean; release build OK; live binary refreshed and setup-hooks rerun (installed ~/.config/git/hooks/pre-commit contains the --local check, grep count 2); all three mirrors at HEAD; ledger line 68 flipped — fixed in d504ab2.

## Audit history

- 2026-08-10T23:48:12.434Z — approved — `opencode-go/deepseek-v4-flash`
