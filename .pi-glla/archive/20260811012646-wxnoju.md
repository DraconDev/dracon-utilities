# Goal

**Status**: aborted
**Policy**: list
**Auto-continue**: on
**File**: `20260811012646-wxnoju.md`
**Stop reason**: already_shipped:already shipped

## Objective

> FIX LOW (warden): ScrubMarkers CLI help says JSON files but scans all protected files (main.rs:211 vs 1790)

## Completion summary

Resolved in 1a2b187 (already shipped this session): the finding was INVALID — scrub_markers (main.rs:1790) SKIPS protected-pattern files via `if protected.is_match(&rel_norm) { continue; }` and only processes `.json` files; this behavior is unchanged since the file's first commit 705f5a8, so the CLI help "Scan plaintext JSON files for DRACON_SECRET markers and optionally scrub them" matched the code all along. The auditor misread build_globset(&policy.protected_patterns) as a scan set when it is the skip set. Resolved by clarifying the CLI help anyway (doc-comment now spells out that "plaintext" = non-protected files and only `.json` is considered, so the ambiguity that generated the false finding is gone). Verified: 110 warden bin tests pass, release build, clippy 0 warnings; warden 3 mirrors at HEAD (1a2b187); ledger line 73 flipped [x] with the full investigation annotation.
