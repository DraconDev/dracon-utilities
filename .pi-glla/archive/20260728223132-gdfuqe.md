# Goal

**Status**: complete
**Policy**: list
**Auto-continue**: on
**File**: `20260728223132-gdfuqe.md`
**Stop reason**: auditor MiniMax-M3 approved

## Objective

> Decide + ship junk-runner ❌ CONCERN remediation. As of 2026-07-28: junk-runner is ❌ CONCERN at 3.79 GiB pushable, blocked from GitHub pushes. The bleed-stop per-repo exclusion is in place (commit `5d6d379d` — `auto_commit_exclude_patterns = [".pi-glla/active.jsonl"]` in `junk-runner/.dracon/dracon-sync.toml`); design doc at `/home/dracon/Dev/dracon-utilities/.pi-glla/notes/junk-runner-pi-glla-bloat-2026-07-28.md`. But the historical 2.884 GiB of `.pi-glla/active.jsonl` blobs (412 distinct copies, measured via `git rev-list --all --objects | grep pi-glla/active.jsonl | xargs git cat-file -s | awk '{sum+=$1}'`) still bloat the pushable branch. Options: (1) `filter-repo --invert-paths --force` to drop all 412 blobs from history + force-push (requires `DRACON_ALLOW_REWRITE=1` for the warden's pre-push hook + daemon pre-rewrite backup branch); (2) orphan github-main cutover (proven unnecessary for platform — retired pattern, not recommended here either); (3) accept the CONCERN indefinitely and document why (operator can still push to gitlab, only GitHub is blocked). Deliverable: pick ONE option and ship it (or explicitly defer with a justification)

## Verification contract

`dracon-sync repos` shows junk-runner at ✅ CLEAN or 🔄 ACTIVE (not ❌ CONCERN) OR a deferred-justification design doc is committed and the operator's decision is recorded.

## Audit history

- 2026-07-28T22:44:41.381Z — approved — `MiniMax-M3`
