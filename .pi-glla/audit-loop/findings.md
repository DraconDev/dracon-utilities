# Audit findings ledger — dracon-utilities (LIST-AUDIT-COLLECT pass, 2026-08-09)
# Scope: dracon-warden (+ dracon-security), dracon-system, dracon-sync, scripts/.
# Append-only: new findings are appended; existing lines are never edited or reordered.

- [x] FIX: HIGH: path_is_protected() multi-component `**/` glob branch is dead code — `path_str.contains("audit/**")` requires a literal `*` inside the path, so `**/secrets/**` never matches any real path; such files still get `filter=dracon` in .gitattributes (git's own globset DOES match `**/`), but the clean gate sees them as unprotected and passes them through UNENCRYPTED (dracon-warden/src/security/src/modules/filter.rs:98-105) — fixed in b4fba45 + ac895d1 (`**/name/**` matches name as a path component at any depth; `**/name` matches only as the final component (glob semantics); multi-component tails like `**/config/services.json` get a substring fallback; old code required a literal `*` in the path so `**/secrets/**` was dead and files passed the clean gate UNENCRYPTED while gitattributes marked them filtered; 6 path_is_protected tests incl. new doublestar suite; revert-proven dead-branch failure; 24 suites ok, clippy clean, hook scan EXIT 0)
- [x] FIX: MEDIUM: whole-file secret-tag detection accepts at most ONE trailing `\n` (single `strip_suffix(b"\n")`), so a whole-file-encrypted BINARY secret whose blob gained CRLF or 2+ trailing newlines falls into the UTF-8-lossy smudge path (U+FFFD corruption of DER/SQLite/.kdbx, re-encrypted into history by next clean) or double-encryption on clean (dracon-warden/src/security/src/lib.rs:856 and 833) — fixed in de343f7
- [x] FIX: MEDIUM: CHANGELOG 0.113.2 documents the BAD_AUTHORS pre-push scan as `git log --first-parent`, but the shipped hook uses `git rev-list --not --remotes` (or `--not $remote_sha`) + per-commit `git log -1` with no --first-parent — the changelog describes an implementation that never shipped; behavior differs (side-merge test identities caught by --not, not by first-parent) (dracon-warden/CHANGELOG.md vs dracon-warden/src/main.rs PRE_PUSH_HOOK) — fixed in cf65d76
- [x] FIX: LOW: warden CHANGELOG ordering — 0.113.2/0.113.3/0.113.4 entries sit BELOW the 0.113.0/0.113.1 entries and below the "All notable changes" boilerplate, so the top of the file presents 0.113.1 as latest while Cargo.toml is 0.113.4 (dracon-warden/CHANGELOG.md) — fixed in d441db1
- [x] FIX: LOW: .gitattributes emits `diff=dracon merge=dracon` but ensure_repo_filter_config registers only filter.dracon.* — no diff/merge drivers are ever defined, so git falls back to the text driver with a warning and encrypted-file merges diff as ciphertext (dracon-warden/src/main.rs:689 vs 953-957) — fixed in 2653d26+9eeb82d+11cd6af+2506eba+0c1ae06+2e90c0b
- [x] FIX: LOW: .env header versioning parses the first "Version: " substring anywhere in the file and gates header-strip on `contains("Dracon Warden")` — an unrelated version line or a comment mentioning Dracon Warden in a fresh .env yields a wrong/duplicated header version (dracon-warden/src/security/src/lib.rs get_env_version/make_env_version_header, modules/filter.rs:214-227) — fixed in fe1179c+2f0594a
- [x] FIX: LOW: apply_link_policy force_replace backups use second-resolution timestamps — two entries with the same basename in one directory within the same second collide and silently overwrite each other's backup (dracon-system/src/links.rs:104-112) — fixed in ea725d3+f528a50
- [x] FIX: LOW: scan_broken_symlinks comment claims `fs::metadata` "doesn't follow symlinks" — it does (that is symlink_metadata); the misleading note invites a future "fix" that would break chain-following detection (dracon-system/src/links.rs:328) — fixed in 850003e+173663e
- [x] FIX: LOW: evaluate_link compares raw (non-canonicalized) paths when both sides fail canonicalize, so equivalent forms like `~/a/../b` vs `~/b` report link_target_mismatch for an in-sync link (dracon-system/src/links.rs:66-72) — fixed in ffab442+671c4c7+e42be8d
- [?] DECIDE: gitignore `!`-negation for protected_patterns is defeated when a parent directory is ignored (git cannot re-include a file under an ignored dir): e.g. hygiene `secrets/` + protected `secrets/**` → encrypted secrets silently never committed. Options: reject ancestor hygiene patterns in validate(), document the interaction, or emit `dir/*` + `!dir/*.age`-style un-ignore pairs (dracon-warden/src/main.rs:655-665)
- [?] DECIDE: pre-push secret scan uses the empty-tree diff for new refs (every line of the whole branch counts as `+`), re-scanning already-published secret-shaped content, while the author scan excludes published commits via `--not --remotes`; align both scopes on --not --remotes or accept the conservative re-scan as defense-in-depth (dracon-warden/src/main.rs PRE_PUSH_HOOK)

# Coverage note: dracon-sync (daemon loop, sync_repo handle_ahead_push fall-through at sync.rs:4230, secrets.rs control-char/perms checks, policy.rs) and scripts/ (release.sh/rotate script use set -euo pipefail) were surveyed; the v0.113.46-0.113.49 changelog claims were verified present in code. No fresh findings for those subsystems.

## Addendum 1 — dracon-sync subsystem (parallel Explore agent)

- [x] FIX: HIGH: byte-index string slicing panics on multi-byte UTF-8 in the goal-metrics commit-message builder — `&reason[..47]`, `&ev[..37]`, `&reason[..37]` guarded only by byte-length checks; a `.pi-glla` goal pause_reason/task evidence with CJK/emoji > 50 bytes panics the daemon's commit path ("byte index 47 is not a char boundary"); report.rs:3729 already uses the safe `chars().take(57)` pattern (dracon-sync/src/sync.rs:2882, 2918, 2947) — fixed in c06853e
- [x] FIX: MEDIUM: AGENTS.md claims `push_op_timeout_secs = 900` "matches the daemon's own code default" — the code default is 300 (policy.rs:1149-1150) and the live config's own comment says "300s matches the daemon's own code default"; operators tuning configs from AGENTS.md get the wrong baseline (AGENTS.md:96-99) — fixed in 48ee05a28
- [x] FIX: LOW: secret-scrubber damage — seven 0-byte `*.rs.plaintext` files are tracked+committed (bump, daemon, release, report, sync, git/mod) and `[DRACON_SECRET:<age-blob>]` markers are embedded mid-word in comments at sync.rs:3435/4361, daemon.rs:1527/1549 (truncating identifiers like `test_sync_repo_mirror_failu[…]`); no runtime strings corrupted, but comment references are erased and the empty files are litter — fixed in 1744497+1ad8fc8+fe49d52
- [x] FIX: LOW: `push_with_retries` comment is factually wrong — "we don't increment `attempt` either" while `continue` in `for attempt in 1..=attempts` DOES advance the counter; the post-pull retry consumes one budget slot (dracon-sync/src/git/push.rs:229-231) — fixed in c85e626
- [x] FIX: LOW: `scale_push_timeout` multiplier branches are dead — with the 300s default, ahead>20 always yields the 600s cap, so the 4×/6× branches (and their "scaling push timeout" log line) can never be observed and mislead operators (dracon-sync/src/sync.rs:177-188) — fixed in 2b4e43e+fb7724c
- [?] DECIDE: per-repo overrides `settling_max_delay_secs` / `dirty_max_age_action` parse into thin air — the SyncPolicy halves are `#[allow(dead_code)]` future-policy, but the RepoPolicyOverride counterparts are consumed NOWHERE with no warning; a repo setting `dirty_max_age_action = "warn"` silently does nothing. Exactly the v0.113.29/33 incident class the v0.113.34 tripwire was built for — the tripwire checks field EXISTENCE, not consumption. Wire them, warn on parse, or drop the override halves (dracon-sync/src/policy.rs:923, 928)

## Addendum 2 — scripts / docs / meta (parallel Explore agent)

- [x] FIX: HIGH: `verify-install.sh` creates a fixture `policy.toml` (`watch_roots = ["$REPO"]`) but never wires it into the binary — `repos --json` runs without `DRACON_SYNC_POLICY` (the env the code honors, policy.rs:1400) and `repos` has no `--policy` flag; the check actually asserts `rows[0]` of the operator's REAL fleet reports `untracked==0`, passing today only because the fleet is clean and false-failing whenever a legitimately-dirty fleet repo is row 0. One-line fix: `DRACON_SYNC_POLICY="$TMP/policy.toml" "$BIN" repos --json` (dracon-sync/scripts/verify-install.sh:47-69) — fixed in 56c70de
- [x] FIX: HIGH: warden's release.sh defaults `REMOTE=github` but the warden repo's remotes are `origin`/`codeberg`/`gitlab` (no `github` — verified `git remote -v`); step 6's `git push "$REMOTE" ...` fails out of the box unless `--remote origin` is remembered. dracon-sync got resolve-github-remote.sh (v0.113.11); warden never did (dracon-warden/scripts/release.sh:59) — fixed in 19aa06a
- [x] FIX: MEDIUM: warden release.sh is a stale pre-v0.113.11 fork of sync's script — inline changelog close has no already-closed idempotency guard (re-run duplicates the `## [version]` header — the exact v0.113.10 bug sync fixed via close-changelog.py), `cargo publish` has no "already exists on crates.io" tolerance, and commit/tag/gh-release steps have no re-run guards (dracon-warden/scripts/release.sh:161-258) — fixed in 68b0b15+929158e
- [x] FIX: MEDIUM: warden release.sh has no post-publish artifact verification — the v0.113.3/0.3.1 protected-patterns incident was caught only by manual publish-verify; sync wires verify-install.sh into step 6 against the packaged crate, warden has no equivalent, and nothing enforces the documented publish order (dracon-security before dracon-warden; the `path` dep masks registry resolution) — fixed in a401cb1+a264034+88a3240
- [x] FIX: MEDIUM: `docs/design/repos-rich-table-columns-2026-07-29.md:222-224` documents auto-pick bands "242-314 → Compact, ≥315 → Full" — the code removed those bands the next day (v0.113.26, report.rs:2726-2752): auto-pick is now only `<165 Compact / ≥165 Rich`; Compact/Full/Vertical are `--layout`-only. Doc contradicts current behavior — fixed in d9cd61fc9
- [x] FIX: MEDIUM: parent CHANGELOG misfiles shipped releases under `## [Unreleased]` (v0.113.47-50, warden v0.113.3/0.3.1) while the last closed header is `[0.113.7] - 2026-07-28` — ~40 releases (0.113.8→0.113.50) never got closed headers; headers also out of order around lines 103-123 (`[0.112.12]` → `[0.112.14]` → `[0.112.13]` → `[0.112.11]`…). Entries exist and are accurate, just not filed under release headers (CHANGELOG.md:8-28) — fixed in 519cea42d
- [x] FIX: LOW: release.sh runs no tests/clippy/deny — its only build gate is `cargo publish --dry-run`; the AGENTS.md "Test discipline" section is a manual precondition, so a single release command can publish a tree that never passed the gates (dracon-sync/scripts/release.sh:198-203) — fixed in c535375+5c730cc
- [x] FIX: LOW: `--abort` help text claims it "Refuses to run if the working tree was already dirty at start" — the abort path performs no such check (dracon-sync/scripts/release.sh:26-28 vs ~:130) — fixed in a6587dc
- [x] FIX: LOW: `~/.dracon/sync-notify/dracon-sync-watchdog.sh:20-21` claims "The release script touches the maintenance-hold marker around the binary swap and removes it afterwards" — no script anywhere references `dracon-sync.maintenance-hold` (grepped all three scripts dirs); an operator who stops the daemon expecting the release script to hold gets it restarted mid-swap ~2.5 min later. (`dracon-sync maintenance` is unaffected — it freezes via `~/.dracon/dracon-sync.freeze`.) — fixed in 68144894a (~/.dracon repo)
- [x] FIX: LOW: both parent audit scripts filter `exclude_dir_names` by exact match, but the live config contains the glob `".tmp-*"` — it never matches during `os.walk`, so `.tmp-*` dirs are scanned by audit-commit-activity.py and verify-ownership-mirrors.py (the daemon presumably globs) — fixed in 6cf1ed156
- [?] DECIDE: parent CHANGELOG policy — close a `## [0.113.x]` header per release (matches the `[0.113.7]` convention) or declare the aggregate-[Unreleased] style intentional and drop the closed-header convention; current state is inconsistent either way
- [?] DECIDE: should release.sh (sync) enforce the test/clippy/deny gates itself (slow, but the "single command" claim becomes true), or keep them as documented manual preconditions? (warden's script currently has neither the gates nor a verify step)

## Coverage note (executor consolidation, 2026-08-09)

The coverage note above the first addendum ("No fresh findings for those subsystems" for sync + scripts) reflects the warden/system survey only — it predates the parallel agents' reports; their sync and scripts findings are listed in Addenda 1-2. Verified-clean areas (from the parallel reports, no action): override tripwire lists consistent (69 SyncPolicy fields, zero gaps/stale entries); v0.113.50 classification matches CHANGELOG; detached-HEAD fix at all three push sites (`HEAD:refs/heads/<branch>`); rewrite-backup bundle sound; stuck-push ledger backoff/retry/exhausted correct; secrets.rs control-char + permission refusals present; status.rs IndexLock O_EXCL correct; AGENTS.md config paths/commands/policy defaults (except the push-timeout wording in Addendum 1) match live config; release.sh step ordering matches the disciplined pipeline; verify-install fixture-check wiring verified in release.sh step 6.

## Addendum 3 — 4-agent parallel collect pass (2026-08-10, v0.113.50/v0.112.36/v0.113.4)

All 42 entries below verified NOT in the ledger above (dedupe done against base + Addenda 1-2). Top-6 claims spot-verified against source by the orchestrator. No code was changed in this pass.

### dracon-sync (agent: sync loop, handle_ahead_push, policy, visibility, exclude, git/*)

- [x] FIX: MEDIUM: `matches_untracked_exclude` still has silently-dead `**` shapes — patterns with a multi-segment tail and no trailing `/**` (`**/scratch/notes.md`) or mid-glob `a/**/b` fall through EVERY branch and never match; operator-excluded files get auto-committed silently (the M28/F3.12 rewrite covered only `**/name` and `**/A/**`); no unit test covers `**/A/B` or `a/**/b` (dracon-sync/src/exclude.rs:1035-1058) — fixed in fc40012 + 5cc2ce8 + 18dba43 (new branch for `**/A/B` multi-segment tails via an end-anchored segment matcher, new rel_matches_doublestar backtracking matcher for mid-glob `a/**/b` incl. multiple `**`; 4 regression suites covering tail, mid-glob, double-mid-glob, glob-tailed cases, including trailing-path overmatch guards; all 4 original cases proven to FAIL pre-fix; 24 workspace suites ok, release builds, clippy delta zero (6 pre-existing daemon/report debt untouched))
- [x] FIX: LOW-MED: `stage_commit_and_push` batch-limit comment claims the cap applies to the union of regular+gitlink paths, but each list is truncated INDEPENDENTLY to `max_batch` → up to 2×max_batch staged in one commit when both classes are present (200 regular + 200 gitlink); dropped entries stay dirty and commit next cycle (latency only, no loss) (dracon-sync/src/sync.rs:3262-3273) — fixed in f66d836
- [x] FIX: LOW: raw git stderr written verbatim to the stuck-push ledger and report HINT column — `record_push_failure` stores `error.to_string()` unredacted (daemon.rs:2602-2621), `handle_ahead_push` eprintln!s it (sync.rs:4399-4412); a `push_url` embedding credentials (`https://user:token@host/`) can land in the ledger/terminal (git masks passwords but prints usernames/URLs in full); redact URLs before write, precedent exists (ownership.rs:468 `redact_origin_credentials`) — fixed in 473efe8
- [x] FIX: LOW: SIGHUP reload + freeze markers polled only at loop top — with `pulse_interval_secs` up to 120s+, a `kill -HUP` remediation or freeze takes up to one full pulse to take effect; the "soft reset" comment promise is stronger than the observed latency (dracon-sync/src/daemon.rs:3524-3576, 3697) — fixed in d337531
- [x] FIX: LOW: stale test comment — report.rs:11460-11462 still describes the removed Vertical (< 220) band; behavior (fallback 120) is correct, code is now Compact <165 / Rich ≥165 (v0.113.26) (dracon-sync/src/report.rs:11460-11462) — fixed in 35c4e4f
- [x] FIX: LOW: visibility freshness windows diverge — `cached_repo_visibility` hardcodes 24 h (visibility.rs:188-196) while `is_visibility_cache_fresh` uses policy `interval_hours` (105-131); both fail closed (stale→None→skip codeberg) so no safety impact, but interval > 24 h makes the sweep re-probe while the gate still accepts day-old data; magic number needs a constant/comment — fixed in 019b2e6

### dracon-warden (agent: hooks, scanner, crypto, filter, team, backup, release.sh, verify-install)

- [x] FIX: MEDIUM: pre-push hook regex requires quotes (`password\s*=\s*["'][^"']+`, main.rs:2645) and the inline scanner's "Generic Secret (Unquoted)" is commented out (scanner.rs:352-355) / "Password Variable (Unquoted)" requires `=` immediately after the name (365-367) → `password = hunter2` in a protected text file (secrets/app.yaml) commits plaintext AND passes the pre-push defense-in-depth; pre-push also never scans binary additions (`git diff` emits no `+` lines for binaries) — fixed in 1c25c66 + 3e48007 + 741159b
- [x] FIX: MEDIUM: whole-file-tag detection is purely syntactic — a protected file whose plaintext begins `[DRACON_SECRET:` and ends `]` trips the clean-side double-encrypt guard (`starts_with_any_secret_tag`, lib.rs:890-930) → committed UNENCRYPTED silently, and `smudge_with_security` propagates the `Some(Err)` on checkout (lib.rs:1213-1221, unlike seal_smudge) → checkout/merge hard-fails; no base64-decode/magic validation distinguishes tag-shaped plaintext (filter.rs:269-272) — fixed in a59db98 (code d4d163f, tests a59db98)
- [x] FIX: MEDIUM: V1 fallback CFB decrypt (`allow_v1_fallback`) is unauthenticated — the only filter is "first 20 bytes ASCII"; a wrong-key decrypt whose CFB output passes the heuristic is returned as silent garbage plaintext (whole-file secret corruption); error path leaks first 20 bytes of ciphertext to stderr (crypto.rs:184-201) — fixed in f8a7c99 + 8999f64 + d7d5532 + dae0e6d
- [x] FIX: MEDIUM: `gather_all_recipients` trusts ANY `*.pub`/`*.key` under `.dracon/data/keys` and `.git/arcane/keys` with no `owner_*`/content validation (unlike the publish path main.rs:874-880) — any contributor who can push a file adds a recipient silently included in every future encryption (decryption access to all subsequent secrets, no review) (crypto.rs:104-133) — fixed in b259b33
- [x] FIX: MEDIUM: global pre-push/pre-rebase hooks shadow repo-local hooks with NO chaining — the H-10 fix covers only pre-commit (main.rs:2461-2464); husky/foreign pre-push+pre-rebase hooks are silently disabled fleet-wide under core.hooksPath (main.rs:2506-2661, 2665-2698); `install_hooks_for_repo` seeds (2805-2848) are dead when global hooksPath is set and swallow errors (`let _ =` main.rs:1213) — fixed in c1f463c
- [x] FIX: MEDIUM: warden release.sh step-3 template ships WRONG install instructions in every release — documents `dracon-warden init` / `clean %f` / `smudge %f` and `filter.dracon-warden.*`; no such subcommands exist (Command enum: Status/Once/ScrubMarkers/Resmudge/Repair/FilterClean/FilterSmudge/Merge/Keygen/SetupHooks); the real contract is `filter-clean`/`filter-smudge` under `filter.dracon.*` (main.rs:978-979) — fixed in 07765be
- [x] FIX: LOW: pre-commit second filter check is dead code — `git config filter.dracon.clean` without `--local` reads the global config the hook's own comment (main.rs:2475-2479) admits carries it → always passes once MANAGED=1 was set; only the `.gitattributes` grep is a real gate (main.rs:2491-2495) — fixed in d504ab2
- [x] FIX: LOW: pre-push secret regex branches untested — tests cover only the AKIA branch + space-filename regression; the WARDEN-M2 single-quote idiom, `-----BEGIN [A-Z]+ PRIVATE KEY`, `password=`/`secret=`/`api_key=` branches have zero shell-level tests; a re-introduction of the `\x27` bug class would go unnoticed (tests.rs:216-265) — fixed in c005204 + 5da73e (auditor-disapproved c005204: vacuous harness + ["''"] collapsed to ["][^"] under shell quoting; 5da73e fixes the regex via the quote idiom and makes the harness non-vacuous - single-quote test proven to fail pre-fix, live clean-room EXIT 0->1; + 8f54108: second disapproval round - the hook doc-comment and tests.rs comment literals still self-matched the (now-fixed) regex, blocking manual pushes of src edits; all comment shapes reworded, hook body verified self-match-free, clean-room hook-only push EXIT 0; + 8e3054f: third round - concat-split fragments landed AFTER the opening quote (secret = 'hunt already matches) and raw literals remained in the security crate (BEGIN PRIVATE KEY regexes, AKIA strings, struct assignment); all re-split with concat!() so the quote/prefix is never a complete match, repo-wide recursive grep = zero, fresh-branch empty-tree..HEAD simulation EXIT 0, fixture-source-lines clean-room EXIT 0, real secret still EXIT 1)
- [x] FIX: LOW: tests.rs:398-401 comment claims the BAD_AUTHORS scan uses `--first-parent`; the hook uses `--not $remote_sha` / `--not --remotes` with no --first-parent (the CHANGELOG entry was fixed in cf65d76, this test comment is a separate unfixed location) — and a branch push of a --no-ff merge with test-identity side commits is still blocked, contradicting the comment's stated intent (tests.rs:398-401 vs main.rs:2642-2646) — fixed in 24a1f3a
- [x] FIX: LOW-MEDIUM: verify-install.sh false success — check 2 passes when `filter-clean` on the protected file ERRORS (empty OUT2 → both `[[ ]]` tests false → "✓ OK"); a filter binary that fails on protected files (e.g. no recipients) reports OK (scripts/verify-install.sh:54-60) — fixed in 9f775c4 (exit-code check before the tag checks; an erroring filter now FAILS with the exit code in the message; proven with a filter stub that works on non-protected paths but exits 3 on key.pem — old code reported OK, new code exits 1)
- [x] FIX: LOW: `.env` re-encryption `.trim()` strips leading/trailing body whitespace on every re-encrypt (not just the header seam, filter.rs:296-301); `strip_env_version_header` finds the marker string ANYWHERE in the file, not only the top block (lib.rs:236-247) — silently rewrites operator formatting — fixed in ce436e3 + 7bfe419 + 3fa19c5 (strip top-6 scoped like is_env_version_managed, filter no longer trims the body, 3 regression tests proven to fail pre-fix) + 2da5d03 (audit 2026-08-11 disapproval round: get_env_version expected "# Version:" IMMEDIATELY after the marker but the real 6-line template puts the "# This file is encrypted..." banner on line 3 - every re-encryption RESET the version to 1 (blob rewritten). Now scans the bounded header window for the version prefix; all fixtures rebuilt to the real template layout (version on line 4); revert-proof: strict adjacency -> tests fail with exactly the reset (0 instead of 1 / Version: 1 not 2); live E2E on real-template v3 -> v4 increment + body byte-exact)
- [x] FIX: LOW: ScrubMarkers CLI help says "Scan plaintext JSON files" but `scrub_markers` scans all protected-pattern files (main.rs:211 vs 1790) — resolved in 1a2b187 (finding was INVALID: scrub_markers SKIPS protected-pattern files via `if protected.is_match { continue }` and only processes .json — unchanged since first commit 705f5a8; help text matched the code all along. CLI help clarified anyway: "plaintext" = non-protected + JSON-only, so the ambiguity that generated the false finding is gone)
- [x] FIX: LOW: tests.rs:1697-1712 (`warden_policy_validate_rejects_secretish_plaintext`) passes only because FORBIDDEN_PLAINTEXT_SUBSTRINGS happens to contain `.env`/`secrets/` first — fragile, not wrong — fixed in 2a0db78 (finding understated: the FORBIDDEN check ran after the allowlist check, making it UNREACHABLE — no allowlisted pattern can contain a forbidden substring, so the guard never fired and secretish patterns got the misleading "allowlisted" error. Order swapped so the specific guard fires first; test now uses real secretish patterns (secrets/app.json, case-insensitive, .env.local) and pins the "secret-ish" message; proven to fail pre-fix with the allowlisted message) + 9f034aa (follow-up: explicitly added `passwords.txt` to FORBIDDEN_PLAINTEXT_SUBSTRINGS and restored it as a regression fixture; removing only the needle makes the test fail with the old allowlist path; workspace tests/release/clippy/deny all pass)
- [x] FIX: LOW: dead code — `seal_clean`/`seal_smudge` (filter.rs:382, 556) have zero callers in the binary; `Warden::smudge` (lib.rs:1195) unused by main.rs — fixed in 3640654+03d2643+0d48fc3 (+4817353/958d351 intermediate daemon commits of the in-flight edit — net effect at HEAD: seal_clean + seal_smudge + backup_secret (their only caller) + Warden struct/impl removed, smudge_with_security kept; comment refs reworded; migrate_markers_in_path walk_errors tail verified intact; 24 workspace suites pass, release builds, clippy clean (dracon-sync 6 pre-existing debt excluded); hook scan EXIT 0)

### scripts / docs / meta (agent: release.sh ×5, close-changelog.py, verify-install ×2, AGENTS.md, CHANGELOGs, design docs, live configs)

- [x] FIX: HIGH: parent `scripts/release.sh:71` hardcodes `REMOTE=github` but this repo's GitHub remote is `origin` (verified `git remote -v`) — step 6b/6c (`git push "$REMOTE" HEAD:main`, `git push "$REMOTE" "$TAG"`, ~lines 449/465) fail out of the box with "fatal: 'github' does not appear to be a git repository"; the v0.113.11 resolve-github-remote.sh treatment was ported to sync and warden but never to the parent script; help text matches the (wrong) default so there is no hint — fixed in 7d8a981b5 + 32881d5d2 (added URL-based `scripts/resolve-github-remote.sh` with canonical-repo preference, made release.sh auto-resolve the GitHub remote while validating explicit `--remote`, updated help, and added fixture tests for origin/canonical preference/no-GitHub failure; real resolver returns `origin`; 16 release assertions pass)
- [x] FIX: HIGH: `dracon-system/scripts/release.sh:33` — `--help` still advertises `--remote` default `github` while the code default is `origin` (line ~62, CHANGED 2026-08-10 v0.112.35) — the one place an operator checks before a release gives the pre-fix, broken advice — fixed in fa1eccb (help now advertises `origin`; `bash -n` passes; live `scripts/release.sh --help` contains the exact origin default and no `(default: github)`)
- [x] FIX: HIGH: `dracon-system/CHANGELOG.md` has live duplicated headers shipped in the CURRENT release — `## [0.112.36] - 2026-08-10` twice (lines 16+18) and glued `## [0.112.35] - 2026-08-10## [0.112.35] - 2026-08-10` (line 52); the system release.sh inline changelog closer has no already-closed guard (the exact v0.113.10 bug class sync/warden fixed via close-changelog.py) and reproduces the corruption on every re-run — fixed in 6bd1f45 + 3b74723 + 69de395 (repaired the live duplicate/glued headers; ported idempotent close-changelog.py and replaced the inline heredoc; added first-close/byte-identical-rerun regression test; ignored Python cache artifacts)
- [x] FIX: MEDIUM: dracon-system release.sh abort path — help line 29 claims it "Refuses to run if the working tree was already dirty at start" but the abort path (~150-175) has no dirty-tree guard; it would silently `git checkout --` pre-existing modified `*.toml`/`CHANGELOG.md` and `rm` untracked `release-notes-v*.md` (sync's release.sh got the real guard 2026-08-10; the system copy only claims it) — fixed in 51bd28b + e9d60bf (help now states the actual release-surface scope; --abort refuses modified/untracked files outside Cargo.toml, CHANGELOG.md, and root release-notes-v*.md before reverting; scratch-repo regression tests prove refusal preserves edits and release-surface-only abort reverts cleanly)
- [x] FIX: MEDIUM: dracon-system release.sh pipeline asymmetry — no test/clippy/deny gates (sync added them 2026-08-10), no idempotent re-run paths (`git commit` errors on empty index, `git tag` errors on existing tag, `gh release create` errors on existing release), no packaged-artifact fixture check, no mirror-remotes reminder — the v0.112.35 manual-completion incident is exactly the class sync/warden hardened against — fixed in f1db8b4 + 0fd6385 + 9d3788d + 7dd085c + a02fd54 + 9e31575 + cdad41d + 72751e1 + a69927c + 624b625 + 93ab7e4 + 2bbe355 + 754aa7a + 911a7ca + d481c0e + aa92a0f (added locked test/build/clippy/deny gates, packaged-binary verify-install fixture, publish/commit/tag/GitHub-release rerun handling, exact mirror-tag reminders, isolated first/second-run regression coverage, synchronized standalone Cargo.lock/deny.toml, standalone lock regeneration as a release surface, clean-clone gate plus next-version locked-preflight coverage, complete dry-run mutation/abort rollback, and changelog documentation)
- [x] FIX: LOW: sync release.sh step 5 under `--dry-run` skips the Cargo.toml bump (step 2), so `cargo publish --dry-run` sanity-checks the OLD version's manifest — a dry-run can pass while the target version's manifest is broken (dracon-sync/scripts/release.sh ~line 331) — fixed in 0e866fc + dddd4e7 (dry-run now writes the requested manifest version; fixture proves the version bump and --abort restoration)
- [x] FIX: LOW: AGENTS.md stale line refs — sync.rs:858,859 (`git add -A` claim; those lines are now file-expansion logic) and policy.rs:1864/1890 for `auto_repair_concerns` (field at 477, default true at 1955); also "verify-install.sh … wired into scripts/release.sh step 6" is actually step 7. Substance verified true; only line numbers drifted — fixed in 90d5a38b4 (refs now point to sync.rs:1079/1097, policy.rs:476-477/1956, and release step 7)

### dracon-system v0.112.36 limiter + legacy paths (agent: main.rs SIGHUP/release paths, doctor, trash, zram, events + warden scanner/team/backup)

- [x] FIX: HIGH: SIGHUP reload restores only the legacy `reniced_pids` map then resets `runtime = GuardRuntimeState::default()` (main.rs:4115-4122) — v0.112.36 `memory_reniced_pids`/`oom_biased_pids`/`capped_pids` are dropped WITHOUT restore: oom-biased pids stay at oom_score_adj=250 permanently (next critical pass reads 250 → `oom_bias_target(250)` → None → never re-tracked), reniced pids stay at nice 5-15, capped pids linger in throttled units; daemon shutdown/restart has no restore hook either (main.rs:917-922) — fixed in 0c05524 + 7a9392e + 33d10b8 + 37d186d + f9098fd + efb8924 + 4f3ccf5 + 1dcd58f + a0155c1 + 5761649 + c9eea1b + a5adb87 + c3c54ec + bac6369 + ccb4713 + 0ec49b3 + 69ca748 + b76031c + 1fbfb70 + dfd2664 (all limiter entries now carry `/proc` starttime identity; SIGHUP/shutdown restore uses comm/starttime matching, distinguishes gone/reused/unavailable processes, retains indeterminate proc/cgroup/systemd failures, does not stop transient services until cgroup state is readable and valid, verifies cgroup/systemd cleanup before removing caps, overlapping legacy/memory renice limiters restore the shared pre-limiter nice value once, PID-reuse entries are identity-aware and preserve the current incarnation, with success/failure/PID-reuse regression coverage, and `guard once` restores limiters on report, JSON, and error paths)
- [x] FIX: MEDIUM: release-path restore failures silently drop tracking — `let _ = renice_process(pid, 0)` (main.rs:2379) and `let _ = fs::write(oom_score_adj)` (main.rs:2404) ignore errors then remove map entries unconditionally; a transient failure (EPERM/exit race) strands the pid at nice>0/adj 250 with NO retry and success messages print regardless; inconsistent with uncap's `continue`-on-error retry (main.rs:2423) (dracon-system/src/main.rs:2379-2382, 2404-2407) — fixed in 37d186d + f9098fd + efb8924 + 4f3ccf5 + 1dcd58f + a0155c1 + a9535f8 (legacy, memory, and OOM release paths now use identity checks, log operation errors, retain entries, and retry on later passes; failure-retention regression coverage exercises failed renice and OOM writes)
- [x] FIX: MEDIUM: legacy heavy-process release compares the 15-char-truncated ps comm (stored `orig_cmd`, main.rs:2839) against the UNTRUNCATED cmdline basename (`exe_name`, main.rs:2927-2944) — any process whose kernel comm was truncated (>15 chars) or whose argv[0] was rewritten is falsely flagged "PID recycled", entry dropped WITHOUT un-renicing; pid stays reniced forever, no retry (dracon-system/src/main.rs:2925-2945) — fixed in 37d186d + f9098fd + efb8924 + 4f3ccf5 + 1dcd58f + a0155c1 + 56266f2 (legacy release now uses stable `/proc` starttime identity rather than cmdline basename; a long-comm regression test confirms the matching process is un-reniced)
- [x] FIX: MEDIUM: memory-renice restore hardcodes nice 0 — the original pre-pressure nice is never captured (map stores only the applied value); a process the user deliberately runs at nice 10 is promoted to 0 on release; the oom path stores/restores the original correctly (dracon-system/src/main.rs:2379, 2839) — fixed in 9f1fdd7 + 531fc1b + 7957c99 + 64256cd (memory limiter now captures both original/applied nice values from `ps ni`, restores the original in release and SIGHUP paths, and tests assert `-n 10` restoration)
- [x] FIX: MEDIUM: `empty_trash` credential guard runs only when `apply && credential_guard` (main.rs:1630) — dry-run (`auto_cleanup_apply=false`, `prune --dry-run`) reports reclaimed bytes the apply run refuses (credential abort, main.rs:1647-1662) and never warns about credential matches; dry-run and apply disagree (dracon-system/src/main.rs:1615-1662) — fixed in dracon-system 9f13f5b + c1da615 (credential scanning now runs for both apply and dry-run, blocks the dry-run reclaim estimate with the same warning/event, and a temp-trash regression test confirms zero reported bytes and preserved files)
- [x] FIX: MEDIUM: SecretScanner `scan()` builds snippets with byte slicing `&matching_str[..60]` (scanner.rs:483-493) — patterns like "Database Password"/"Generic Secret" (`[^"']{8,}`) can match multi-byte UTF-8, so a 60+ byte secret with CJK/emoji inside the first 60 bytes PANICS the scanner mid-hook ("byte index 60 is not a char boundary"); same class as the sync fix c06853e in this ledger, here unfixed (dracon-warden/src/security/src/modules/scanner.rs:483-493) — fixed in dracon-warden aec2142 + c78f726 + 8d4504f (snippet truncation now floors at a valid UTF-8 character boundary; regression coverage scans a long emoji-valued Generic Secret; unrelated formatter drift was reverted and the changelog records the fix)
- [x] FIX: MEDIUM: "DSA/EC/OpenSSH/PGP Private Key" patterns are copy-paste duplicates of the RSA regex (scanner.rs:318-341) — DSA/EC/OpenSSH/PGP keys are never reported under their own names (the generic `[A-Z ]+` pattern catches most and mislabels them "RSA Private Key"), and armored `-----BEGIN PGP PRIVATE KEY BLOCK-----` matches NO pattern at all (missed entirely) — fixed in dracon-warden cfb27b3 + fe5a0da (format-specific DSA/EC/OpenSSH/PGP regexes, including PGP block armor, plus specific-name regression coverage)
- [x] FIX: LOW: warden `backup_file` timestamps at second resolution (`as_secs()`, backup.rs:42-45) — two backups of the same file within one second produce the identical `<hash>_<secs>.age` filename and `fs::write` silently overwrites the earlier backup (data loss); same class as the ledger's fixed links.rs finding, still unfixed (dracon-warden/src/security/src/modules/backup.rs:42-52) — fixed in dracon-warden 0b7ef4e + bb72bc8 (nanosecond timestamp names, exclusive create/retry collision handling, deterministic helper test, and rapid backup retention/restore regression)
- [x] FIX: LOW: doctor table STATUS column renders "present" for every FAILING check (`if *ok { "ok" } else { "present" }`, doctor.rs:167-169) — inverted label; `system_root_exists` is computed but never displayed (dracon-system/src/doctor.rs:107-171) — fixed in dracon-system f95a957 (failing rows now render `fail`, and the `~/.dracon` system-root row is displayed with regression coverage)
- [x] FIX: LOW: doc/code drift — "INVARIANT: This function is the ONLY process management action the guard takes... only renices. Killing is explicitly banned" (main.rs:870-871) is false since v0.112.36 (oom_score_adj steering + CPUQuota units); `check_memory_pressure` doc (main.rs:2177-2181) omits the limiting subsystem (dracon-system/src/main.rs:870-871, 2177-2181) — fixed in dracon-system 8967e94 + parent ae2ae4038 (source comments, utility README, and root `docs/OPERATIONS.md` now document renice, OOM bias, and CPUQuota throttling; `guard clean` is correctly documented as reclaimable-space cleanup, not process rollback; changelogs updated)
- [x] FIX: LOW: oom bias inherited by forked children — a biased process that forks during the window leaves descendants at oom_score_adj=250 permanently; the release identity check (main.rs:2395-2400) only covers the tracked pid; no sweep for stranded 250s (dracon-system/src/main.rs:2390-2407) — fixed in dracon-system d5b185f + 45cbd6b + abe710c + cb99b3e + 748553e + a1fa45f + 93c8d7d + eea60a2 + cd9406b (tracks pre-bias descendant incarnations, defers failed/unreadable child restoration without dropping root state, assigns nested descendants to the nearest biased ancestor, restores to the correct pre-bias adjustment, and adds retry/nested-root regressions; system changelog updated)
- [x] FIX: LOW: `cap_offenders_cpu_percent` unclamped in normalize_guard_policy — a value >100 passes normalization, then `cap_cpu_process` rejects it every pass → `⚠️ cpu-cap failed` spam every interval and the pid is never capped (dracon-system/src/main.rs:943-944, 3351-3389) — fixed in dracon-system a4bdaa4: normalize values above 100 to the maximum one-CPU cap and add regression coverage for oversized configuration values; changelog updated
- [x] FIX: LOW: `prev_swap_counters` stores pswpout as literal 0 — parsed then discarded; comment claims "last pswpin/pswpout counters" (dracon-system/src/main.rs:2218-2224, 428-431) — fixed in dracon-system d829715: the fallback records parsed `pswpout` alongside `pswpin`, with regression coverage and changelog documentation
- [x] FIX: LOW: `create_team` writes the encrypted team key with default umask perms (team.rs:88 `std::fs::write`) while `accept_team_invite` correctly uses 0o600 + create_new (team.rs:176-190) — inconsistent; the create path also lacks the overwrite guard (a re-run overwrites the existing team key instead of erroring) (dracon-warden/src/security/src/modules/team.rs:88, 176-190) — fixed in dracon-warden 0506256 + 447dd85 (shared exclusive private-file helper uses create_new and Unix mode 0600 for create and invite paths; regression verifies permissions and repeated-create preservation; changelog updated; unrelated formatter drift reverted in 536571b)
- [?] DECIDE: `bias_oom_on_pressure` is default-true but deliberately makes the kernel MORE likely to kill the biased process — contradicts the documented invariant "The guard NEVER kills processes" (main.rs:870-871). Options: (a) keep default-true and amend the invariant docs to carve out kill-steering, or (b) default it off so the kill-adjacent behavior is explicit opt-in (dracon-system/src/main.rs:870-871, policy.rs:136-143)
