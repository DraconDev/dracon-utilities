# Audit findings ledger — dracon-utilities (LIST-AUDIT-COLLECT pass, 2026-08-09)
# Scope: dracon-warden (+ dracon-security), dracon-system, dracon-sync, scripts/.
# Append-only: new findings are appended; existing lines are never edited or reordered.

- [ ] FIX: HIGH: path_is_protected() multi-component `**/` glob branch is dead code — `path_str.contains("audit/**")` requires a literal `*` inside the path, so `**/secrets/**` never matches any real path; such files still get `filter=dracon` in .gitattributes (git's own globset DOES match `**/`), but the clean gate sees them as unprotected and passes them through UNENCRYPTED (dracon-warden/src/security/src/modules/filter.rs:98-105)
- [x] FIX: MEDIUM: whole-file secret-tag detection accepts at most ONE trailing `\n` (single `strip_suffix(b"\n")`), so a whole-file-encrypted BINARY secret whose blob gained CRLF or 2+ trailing newlines falls into the UTF-8-lossy smudge path (U+FFFD corruption of DER/SQLite/.kdbx, re-encrypted into history by next clean) or double-encryption on clean (dracon-warden/src/security/src/lib.rs:856 and 833) — fixed in de343f7
- [x] FIX: MEDIUM: CHANGELOG 0.113.2 documents the BAD_AUTHORS pre-push scan as `git log --first-parent`, but the shipped hook uses `git rev-list --not --remotes` (or `--not $remote_sha`) + per-commit `git log -1` with no --first-parent — the changelog describes an implementation that never shipped; behavior differs (side-merge test identities caught by --not, not by first-parent) (dracon-warden/CHANGELOG.md vs dracon-warden/src/main.rs PRE_PUSH_HOOK) — fixed in cf65d76
- [x] FIX: LOW: warden CHANGELOG ordering — 0.113.2/0.113.3/0.113.4 entries sit BELOW the 0.113.0/0.113.1 entries and below the "All notable changes" boilerplate, so the top of the file presents 0.113.1 as latest while Cargo.toml is 0.113.4 (dracon-warden/CHANGELOG.md) — fixed in d441db1
- [x] FIX: LOW: .gitattributes emits `diff=dracon merge=dracon` but ensure_repo_filter_config registers only filter.dracon.* — no diff/merge drivers are ever defined, so git falls back to the text driver with a warning and encrypted-file merges diff as ciphertext (dracon-warden/src/main.rs:689 vs 953-957) — fixed in 2653d26+9eeb82d+11cd6af+2506eba+0c1ae06+2e90c0b
- [x] FIX: LOW: .env header versioning parses the first "Version: " substring anywhere in the file and gates header-strip on `contains("Dracon Warden")` — an unrelated version line or a comment mentioning Dracon Warden in a fresh .env yields a wrong/duplicated header version (dracon-warden/src/security/src/lib.rs get_env_version/make_env_version_header, modules/filter.rs:214-227) — fixed in fe1179c+2f0594a
- [x] FIX: LOW: apply_link_policy force_replace backups use second-resolution timestamps — two entries with the same basename in one directory within the same second collide and silently overwrite each other's backup (dracon-system/src/links.rs:104-112) — fixed in ea725d3+f528a50
- [x] FIX: LOW: scan_broken_symlinks comment claims `fs::metadata` "doesn't follow symlinks" — it does (that is symlink_metadata); the misleading note invites a future "fix" that would break chain-following detection (dracon-system/src/links.rs:328) — fixed in 850003e+173663e
- [x] FIX: LOW: evaluate_link compares raw (non-canonicalized) paths when both sides fail canonicalize, so equivalent forms like `~/a/../b` vs `~/b` report link_target_mismatch for an in-sync link (dracon-system/src/links.rs:66-72) — fixed in ffab442+671c4c7+e42be8d
- [?] DECIDE: gitignore `!`-negation for protected_patterns is defeated when a parent directory is ignored (git cannot re-include a file under an ignored dir): e.g. hygiene `secrets/` + protected `secrets/**` → encrypted secrets silently never committed. Options: reject ancestor hygiene patterns in validate(), document the interaction, or emit `dir/*` + `!dir/*.age`-style un-ignore pairs (dracon-warden/src/main.rs:655-665)
- [?] DECIDE: pre-push secret scan uses the empty-tree diff for new refs (every line of the whole branch counts as `+`), re-scanning already-published secret-shaped content, while the author scan excludes published commits via `--not --remotes`; align both scopes on --not --remotes or accept the conservative re-scan as defense-in-depth (dracon-warden/src/main.rs PRE_PUSH_HOOK)

# Coverage note: dracon-sync (daemon loop, sync_repo handle_ahead_push fall-through at sync.rs:4230, secrets.rs control-char/perms checks, policy.rs) and scripts/ (release.sh/rotate script use set -euo pipefail) were surveyed; the v0.113.46-0.113.49 changelog claims were verified present in code. No fresh findings for those subsystems.

## Addendum 1 — dracon-sync subsystem (parallel Explore agent)

- [x] FIX: HIGH: byte-index string slicing panics on multi-byte UTF-8 in the goal-metrics commit-message builder — `&reason[..47]`, `&ev[..37]`, `&reason[..37]` guarded only by byte-length checks; a `.pi-glla` goal pause_reason/task evidence with CJK/emoji > 50 bytes panics the daemon's commit path ("byte index 47 is not a char boundary"); report.rs:3729 already uses the safe `chars().take(57)` pattern (dracon-sync/src/sync.rs:2882, 2918, 2947) — fixed in c06853e
- [x] FIX: MEDIUM: AGENTS.md claims `push_op_timeout_secs = 900` "matches the daemon's own code default" — the code default is 300 (policy.rs:1149-1150) and the live config's own comment says "300s matches the daemon's own code default"; operators tuning configs from AGENTS.md get the wrong baseline (AGENTS.md:96-99) — fixed in 48ee05a28
- [x] FIX: LOW: secret-scrubber damage — seven 0-byte `*.rs.plaintext` files are tracked+committed (bump, daemon, release, report, sync, git/mod) and `[DRACON_SECRET:<age-blob>]` markers are embedded mid-word in comments at sync.rs:3435/4361, daemon.rs:1527/1549 (truncating identifiers like `test_sync_repo_mirror_failu[…]`); no runtime strings corrupted, but comment references are erased and the empty files are litter — fixed in 1744497+1ad8fc8+fe49d52
- [x] FIX: LOW: `push_with_retries` comment is factually wrong — "we don't increment `attempt` either" while `continue` in `for attempt in 1..=attempts` DOES advance the counter; the post-pull retry consumes one budget slot (dracon-sync/src/git/push.rs:229-231) — fixed in c85e626
- [x] FIX: LOW: `scale_push_timeout` multiplier branches are dead — with the 300s default, ahead>20 always yields the 600s cap, so the 4×/6× branches (and their "scaling push timeout" log line) can never be observed and mislead operators (dracon-sync/src/sync.rs:177-188) — fixed in 2b4e43e+fb7724c
- [?] DECIDE: per-repo overrides `settling_max_delay_secs` / `dirty_max_age_action` parse into thin air — the SyncPolicy halves are `#[allow(dead_code)]` future-policy, but the RepoPolicyOverride counterparts are consumed NOWHERE with no warning; a repo setting `dirty_max_age_action = "warn"` silently does nothing. Exactly the v0.113.29/33 incident class the v0.113.34 tripwire was built for — the tripwire checks field EXISTENCE, not consumption. Wire them, warn on parse, or drop the override halves (dracon-sync/src/policy.rs:923, 928)

## Addendum 2 — scripts / docs / meta (parallel Explore agent)

- [x] FIX: HIGH: `verify-install.sh` creates a fixture `policy.toml` (`watch_roots = ["$REPO"]`) but never wires it into the binary — `repos --json` runs without `DRACON_SYNC_POLICY` (the env the code honors, policy.rs:1400) and `repos` has no `--policy` flag; the check actually asserts `rows[0]` of the operator's REAL fleet reports `untracked==0`, passing today only because the fleet is clean and false-failing whenever a legitimately-dirty fleet repo is row 0. One-line fix: `DRACON_SYNC_POLICY="$TMP/policy.toml" "$BIN" repos --json` (dracon-sync/scripts/verify-install.sh:47-69) — fixed in 56c70de
- [x] FIX: HIGH: warden's release.sh defaults `REMOTE=github` but the warden repo's remotes are `origin`/`codeberg`/`gitlab` (no `github` — verified `git remote -v`); step 6's `git push "$REMOTE" ...` fails out of the box unless `--remote origin` is remembered. dracon-sync got resolve-github-remote.sh (v0.113.11); warden never did (dracon-warden/scripts/release.sh:59) — fixed in 19aa06a
- [x] FIX: MEDIUM: warden release.sh is a stale pre-v0.113.11 fork of sync's script — inline changelog close has no already-closed idempotency guard (re-run duplicates the `## [version]` header — the exact v0.113.10 bug sync fixed via close-changelog.py), `cargo publish` has no "already exists on crates.io" tolerance, and commit/tag/gh-release steps have no re-run guards (dracon-warden/scripts/release.sh:161-258) — fixed in 68b0b15+929158e
- [x] FIX: MEDIUM: warden release.sh has no post-publish artifact verification — the v0.113.3/0.3.1 protected-patterns incident was caught only by manual publish-verify; sync wires verify-install.sh into step 6 against the packaged crate, warden has no equivalent, and nothing enforces the documented publish order (dracon-security before dracon-warden; the `path` dep masks registry resolution) — fixed in a401cb1+a264034+88a3240
- [x] FIX: MEDIUM: `docs/design/repos-rich-table-columns-2026-07-29.md:222-224` documents auto-pick bands "242-314 → Compact, ≥315 → Full" — the code removed those bands the next day (v0.113.26, report.rs:2726-2752): auto-pick is now only `<165 Compact / ≥165 Rich`; Compact/Full/Vertical are `--layout`-only. Doc contradicts current behavior — fixed in d9cd61fc9
- [x] FIX: MEDIUM: parent CHANGELOG misfiles shipped releases under `## [Unreleased]` (v0.113.47-50, warden v0.113.3/0.3.1) while the last closed header is `[0.113.7] - 2026-07-28` — ~40 releases (0.113.8→0.113.50) never got closed headers; headers also out of order around lines 103-123 (`[0.112.12]` → `[0.112.14]` → `[0.112.13]` → `[0.112.11]`…). Entries exist and are accurate, just not filed under release headers (CHANGELOG.md:8-28) — fixed in 519cea42d
- [x] FIX: LOW: release.sh runs no tests/clippy/deny — its only build gate is `cargo publish --dry-run`; the AGENTS.md "Test discipline" section is a manual precondition, so a single release command can publish a tree that never passed the gates (dracon-sync/scripts/release.sh:198-203) — fixed in c535375+5c730cc
- [x] FIX: LOW: `--abort` help text claims it "Refuses to run if the working tree was already dirty at start" — the abort path performs no such check (dracon-sync/scripts/release.sh:26-28 vs ~:130) — fixed in a6587dc
- [x] FIX: LOW: `~/.dracon/sync-notify/dracon-sync-watchdog.sh:20-21` claims "The release script touches the maintenance-hold marker around the binary swap and removes it afterwards" — no script anywhere references `dracon-sync.maintenance-hold` (grepped all three scripts dirs); an operator who stops the daemon expecting the release script to hold gets it restarted mid-swap ~2.5 min later. (`dracon-sync maintenance` is unaffected — it freezes via `~/.dracon/dracon-sync.freeze`.) — fixed in 68144894a (~/.dracon repo)
- [ ] FIX: LOW: both parent audit scripts filter `exclude_dir_names` by exact match, but the live config contains the glob `".tmp-*"` — it never matches during `os.walk`, so `.tmp-*` dirs are scanned by audit-commit-activity.py and verify-ownership-mirrors.py (the daemon presumably globs)
- [?] DECIDE: parent CHANGELOG policy — close a `## [0.113.x]` header per release (matches the `[0.113.7]` convention) or declare the aggregate-[Unreleased] style intentional and drop the closed-header convention; current state is inconsistent either way
- [?] DECIDE: should release.sh (sync) enforce the test/clippy/deny gates itself (slow, but the "single command" claim becomes true), or keep them as documented manual preconditions? (warden's script currently has neither the gates nor a verify step)

## Coverage note (executor consolidation, 2026-08-09)

The coverage note above the first addendum ("No fresh findings for those subsystems" for sync + scripts) reflects the warden/system survey only — it predates the parallel agents' reports; their sync and scripts findings are listed in Addenda 1-2. Verified-clean areas (from the parallel reports, no action): override tripwire lists consistent (69 SyncPolicy fields, zero gaps/stale entries); v0.113.50 classification matches CHANGELOG; detached-HEAD fix at all three push sites (`HEAD:refs/heads/<branch>`); rewrite-backup bundle sound; stuck-push ledger backoff/retry/exhausted correct; secrets.rs control-char + permission refusals present; status.rs IndexLock O_EXCL correct; AGENTS.md config paths/commands/policy defaults (except the push-timeout wording in Addendum 1) match live config; release.sh step ordering matches the disciplined pipeline; verify-install fixture-check wiring verified in release.sh step 6.
