# Goal

**Status**: active
**Policy**: goal
**Auto-continue**: on

## Objective

> Ship a clean doc surface + the just-deployed versions, with three concrete deliverables:

1. **Tag the three just-deployed versions** in each nested standalone git repo:
   - dracon-sync → v0.113.4 (latest of the 0.113.2/3/4 batch; the 0.113.2 + 0.113.3 tags are optional if you want them — only v0.113.4 is mandatory since it's the live binary)
   - dracon-warden → v0.113.1
   - dracon-system → v0.112.34
   Use the existing nested-repo tag convention (lightweight tags pushed to the three remotes). Update each package's `[Unreleased]` section to point at the new release as the latest, and create `release-notes-v0.113.4.md` / `v0.113.1.md` / `v0.112.34.md` in each package's dir per the existing per-utility release-notes convention.

2. **Doc-polish pass** on the meta-repo:
   - Fix the AGENTS.md "Recent audit-driven changes (2026-07-19, v0.112.21)" section (audit B8) to extend through v0.113.0 (enforcement stack) and v0.113.4 (full-audit remediation).
   - Sweep every workspace-root .md (AGENTS.md, CHANGELOG.md, AUDIT_FULL_2026-07-26.md, docs/README.md, docs/design/*) for stale version numbers and references to code locations that were removed in v0.113.0–v0.113.4 (e.g. the removed `hooks.rs` referenced in some audit detail files, the listed `report_v2_snapshot.rs:3166` line that no longer exists). Fix any citation that's now wrong.
   - Add a short "Latest versions" matrix at the top of `dracon-utilities/README.md` showing the three utility versions + their role.

3. **Archive superseded audits** at workspace root to `docs/archive/audits-2026-07/`, matching the existing audit-archive convention. Targets: `AUDIT-3-UTILITIES-*.md`, `AUDIT_REPOS_*.md`, `AUDIT_FULL_2026-07-18*`, `AUDIT_FULL_2026-07-21*`. The canonical "latest" audit stays at workspace root as `AUDIT_FULL_2026-07-26.md`.

Hard out-of-scope: any code changes, any new findings (those stay in the audit backlog list), the crates.io publish of dracon-git (needs CARGO_REGISTRY_TOKEN from you), the warden 10 MiB question (already resolved via option 1 earlier this session).

No force-push anywhere. No new tag for `dracon-utilities/` workspace root (its last tag is v1.0.0 and stays as the global stamp).

## Verification contract

Done when:
1. `git -C /home/dracon/Dev/dracon-utilities/dracon-sync tag --list | grep '^v0.113.4$'` exits 0 (tag exists). 2. `git -C /home/dracon/Dev/dracon-utilities/dracon-warden tag --list | grep '^v0.113.1$'` exits 0. 3. `git -C /home/dracon/Dev/dracon-utilities/dracon-system tag --list | grep '^v0.112.34$'` exits 0. 4. Each tag is pushed to all three remotes (codeberg/github/gitlab): `git -C dracon-sync ls-remote --tags codeberg | grep v0.113.4` (and the other 5 remote/tag pairs) is non-empty. 5. `dracon-sync/release-notes-v0.113.4.md`, `dracon-warden/release-notes-v0.113.1.md`, `dracon-system/release-notes-v0.112.34.md` exist at those paths. 6. `grep -c "v0.112.21" /home/dracon/Dev/dracon-utilities/AGENTS.md` is 0 (the doc-rot section was extended, not stuck on v0.112.21). 7. `grep -rE "hooks\.rs|report_v2_snapshot\.rs:3166" /home/dracon/Dev/dracon-utilities/*.md /home/dracon/Dev/dracon-utilities/docs/design/` returns no audit-style citation of removed code (the docs are accurate). 8. `head -20 /home/dracon/Dev/dracon-utilities/README.md` contains a "Latest versions" matrix naming sync 0.113.4 / warden 0.113.1 / system 0.112.34. 9. `ls /home/dracon/Dev/dracon-utilities/docs/archive/audits-2026-07/` contains files that previously were at workspace root: AUDIT-3-UTILITIES-*, AUDIT_REPOS_*, AUDIT_FULL_2026-07-18*, AUDIT_FULL_2026-07-21*. 10. `ls /home/dracon/Dev/dracon-utilities/AUDIT-3-UTILITIES*.md /home/dracon/Dev/dracon-utilities/AUDIT_REPOS*.md /home/dracon/Dev/dracon-utilities/AUDIT_FULL_2026-07-18*.md /home/dracon/Dev/dracon-utilities/AUDIT_FULL_2026-07-21*.md 2>&1 | grep -c "No such file"` is the count of moved files (each one absent).
