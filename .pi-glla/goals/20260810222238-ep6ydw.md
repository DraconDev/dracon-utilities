# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260810222238-ep6ydw.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msnt3x8r-5ad0fs`

## Objective

> FIX LOW (sync): visibility freshness windows diverge — cached_repo_visibility hardcodes 24h vs is_visibility_cache_fresh policy interval_hours (visibility.rs:188-196 vs 105-131)

## Completion summary

FIX LOW (sync) shipped: the visibility freshness divergence is gone. cached_repo_visibility now takes interval_hours (the policy value sync_visibility_interval_hours) instead of a hardcoded 24h, matching is_visibility_cache_fresh; interval_hours is threaded through codeberg_push_excluded_for_repo, effective_codeberg_auto_create_override, push_mirror_remotes, push_mirror_remotes_create_only, and auto_create_all_remotes. All 14 production call sites pass policy.sync_visibility_interval_hours (daemon x5, report x5, sync x2, multi_remote x2); tests pass 24. A new unit test proves a 2h-old cache is accepted at the 24h interval but stale at 1h, with the boundary matching the sweep's strict < comparison. Commits 5e119ae (daemon/report/git-mod), 2fbe9ab (multi_remote), 019b2e6 (visibility.rs + new test) — all three mirrors (github origin, codeberg, gitlab) verified at HEAD 019b2e6. Gates green: release + dev builds with --locked, 947 bin unit tests pass (0 fail), clippy --locked -- -D warnings clean. Ledger line flipped [x] — fixed in 019b2e6 on the same line (append-only discipline).
