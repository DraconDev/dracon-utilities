# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260811171155-sb1zpw.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msoxdfvh-thvmt7`

## Objective

> FIX MEDIUM (system): empty_trash credential guard only in apply mode — dry-run reports bytes apply refuses; align dry-run warning (main.rs:1615-1662)

## Completion summary

Fixed the system trash credential-guard mismatch. Credential-like filename scanning now runs whenever `trash_credential_guard` is enabled, including dry-run mode, so a dry run no longer reports reclaimable bytes that an apply run would refuse. The existing warning and `trash/credential-guard` event are shared by both modes, while dry-run still leaves all files untouched.
