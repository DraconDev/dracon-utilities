# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on
**File**: `20260728012220-o3273t.md`

## Objective

> **browser-ext virtual-pet loop decision: keep, constrain, or stop.**

Investigate whether the virtual-pet agent loop in `/home/dracon/Dev/browser-extensions-shared/extensions/standalones/virtual-pet/` is still running, and decide:
- **Keep** (current state): loop continues, each commit becomes an audit-able agent round.
- **Constrain**: e.g. rate-limit to N commits/hour, require approval for non-doc changes, scope to specific paths only.
- **Stop**: write a `.pi-glla/.pause-auto-commit` sentinel for the repo or remove the loop entirely; preserve goal files in `extensions/standalones/virtual-pet/.pi-glla/archive/`.

**Verification contract:**
- Decision recorded in `/home/dracon/Dev/dracon-utilities/docs/design/browser-ext-virtual-pet-loop-decision-2026-07-27.md` (or `.pi-tmp/`).
- If "stop": loop process confirmed not running (`ps aux | grep virtual-pet` empty), no new commits in `/home/dracon/Dev/browser-extensions-shared` after the stop time, daemon `repos` view stays clean for this repo.
- If "constrain": constraint mechanism active and verified working (test push of unauthorized change is rejected).
- If "keep": explicit AGENTS.md note acknowledging the loop is intentional and listing its trust scope.

**Hard out-of-scope:** rewriting the virtual-pet extension itself, force-pushing existing history, modifying warden's hook layer.
</item>
