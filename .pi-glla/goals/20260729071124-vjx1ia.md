# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on

## Objective

> dracon-sync repos table: built-in legend footer — print a short legend under every `dracon-sync repos` table explaining each column (STATUS, ACTIVITY, A/B, PUSH, USED, COMMITS, SIZE, TOUCHED) and the color semantics (SIZE yellow ≥ 1 GiB, red = github 2 GiB push risk; USED green/yellow/white/black tiers), 1-2 lines per column, terminal-width aware

## Verification contract

`dracon-sync repos` output ends with the legend block, legend is suppressed or wraps cleanly under narrow terminals (no broken table), existing table snapshot/unit tests updated, cargo test --workspace --locked + clippy -D warnings green.
