# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260811220205-649ccp.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msp7rtkv-svxkhi`

## Objective

> Fix audit finding: MEDIUM: `matches_untracked_exclude` still has silently-dead `**` shapes — patterns with a multi-segment tail and no trailing `/**` (`**/scratch/notes.md`) or mid-glob `a/**/b` fall through EVERY branch and never match; operator-excluded files get auto-committed silently (the M28/F3.12 rewrite covered only `**/name` and `**/A/**`); no unit test covers `**/A/B` or `a/**/b` (dracon-sync/src/exclude.rs:1035-1058) —

## Completion summary

The `matches_untracked_exclude` finding is fixed. Multi-segment `**/A/B` tails now match only when the explicit tail reaches the path end, while `**/A/**` retains contains semantics and mid-glob `**` uses anchored backtracking. Regression coverage includes the reported shapes and trailing-path overmatch guards.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").
