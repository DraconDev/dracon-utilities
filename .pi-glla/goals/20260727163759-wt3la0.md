# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260727163759-wt3la0.md`
**Pause reason**: auditor disapproved
**Agent suggests**: Inspect auditor feedback and fix the actual gap before calling complete_goal again

## Objective

> **Audit MEDIUM batch (2026-07-26 follow-up): fix the 4 still-open SYNC MEDIUMs in `dracon-sync`.** 

Scope (from `.pi-tmp/audit-2026-07-26-part1-daemon-sync.md` + `AUDIT_FULL_2026-07-26.md`):
- **M1** `detached_discard` (`daemon.rs:4027-4035` + `4158-4165`) is keyed per-repo, not per-task-generation → a fresh result can be discarded and a stale one applied. Patch to key on `(repo, task_generation)`.
- **M2** Filter-only early return drops injected stale-gitlink entries → parent gitlink convergence starved for filter-noisy parents. Patch to defer gitlink injection until after `filter_only_cleared` is recomputed, or to re-inject after the return.
- **M3** v0.113.1 FilterOnly `handle_ahead_push` path can flip a benign repo to PushFailed / stuck-ledger exhaustion for pushes that were never needed. Gate `handle_ahead_push` on FilterOnly being stable for ≥ N cycles before re-attempting (matches v0.113.1 design intent).
- **M4** Main apply phase vs trailing-drain asymmetry for identical outcomes. Document or fix the divergence.

**Verification contract:**
- `cargo test --workspace --locked` ≥ 1038 + N new tests pass (where N = M1+M2+M3+M4 fixes).
- `cargo clippy --workspace --locked --all-targets -- -D warnings` exit 0.
- `cargo build --release --locked` clean.
- Each fix has a regression test that reproduces the original bug before applying the patch (proof the test would catch a regression).
- A `release-notes-v0.113.5.md` (or `vNEXT`) covers the 4 fixes with line refs to each M-finding remediation.
- After release, `dracon-sync repos` shows 36 repos · ✅ CLEAN 31+ · 🔄 ACTIVE 5 · 🟡 WARN 0 · ❌ CONCERN 0 (no regressions).
- Out of scope: LOW batch, audit re-run.

**Hard out-of-scope:** LOW findings, any 1.0-rolling concerns, history-rewrite without operator OK.
</item>

## Tasks

- [x] Read M1-M4 audit details + cite exact source locations `1`
  - [ ]  `1.1`
  - [ ]  `1.2`
  - [ ]  `1.3`
  - [ ]  `1.4`
  - [ ]  `1.5`
- [x] M1 detached_discard: rekey on (repo, task_generation) `2`
  - [ ]  `2.1`
  - [ ]  `2.2`
  - [ ]  `2.3`
- [x] M2 Filter-only gitlink starvation: re-inject after early return `3`
  - [ ]  `3.1`
  - [ ]  `3.2`
  - [ ]  `3.3`
- [x] M3 FilterOnly handle_ahead_push gating: stability check before re-attempt `4`
  - [ ]  `4.1`
  - [ ]  `4.2`
  - [ ]  `4.3`
- [x] M4 Apply vs trailing-drain: document or fix asymmetry `5`
  - [ ]  `5.1`
  - [ ]  `5.2`
- [x] Final gates: tests + clippy + build + release-notes + ship `6`
  - [ ]  `6.1`
  - [ ]  `6.2`
  - [ ]  `6.3`
  - [ ]  `6.4`

## Audit history

- 2026-07-27T22:50:57.681Z — disapproved — `MiniMax-M3`
