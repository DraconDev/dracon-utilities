# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on
**File**: `20260804220700-bsln1j.md`
**Pause reason**: auditor disapproved on quota-retry — see /goal status

## Objective

> Restore and harden dracon-sync fleet synchronization end-to-end. Re-establish the canonical source checkout if needed, then safely snapshot and reconcile the corrupted `ai-auto-writer` checkout from its verified healthy remote without deleting worktree files or rewriting published history; repair `darklord` and the loop identity workflow; migrate ownership to path-owned-by-default (`owned = false` is the explicit opt-out and legacy `owned = true` remains compatible), treating untrusted authors as warnings rather than synchronization gates and claiming/provisioning empty initialized watched repositories. Fix reporting so invalid Git history is not shown as `EMPTY_REPO` and ownership-skipped repositories are not shown as active `PENDING` pushes. Implement private-by-default forge provisioning, writable pushes only to configured operator namespaces, foreign remotes as fetch-only warnings, and public-positive Codeberg auto-mirroring (unknown/API-failure visibility never publishes; private-everywhere repositories stop new Codeberg pushes but retain existing mirrors). Make every permitted configured remote converge to the chosen version through normal fast-forward/merge reconciliation, stopping on conflicts rather than rewriting published history. Audit and document commit-activity changes with explanations and remediation of real blockers, without manufacturing commits. Run the full test/release/install/restart cycle and verify live behavior across the affected fleet.

## Verification contract

Done when:
`cargo test --workspace --locked` passes.
`cargo clippy --workspace --locked -- -D warnings` passes.
`cargo deny check` passes.
`python3 scripts/audit-commit-activity.py --compare-days 3 --write docs/design/commit-activity-audit.md` exits successfully and writes the dated activity audit.
`python3 scripts/verify-ownership-mirrors.py` exits successfully against the live configured repositories and remotes.
`test -s docs/design/ownership-and-codeberg-auto-provisioning.md && test -s docs/design/commit-activity-audit.md` succeeds.
`git -C /home/dracon/Dev/ai-auto-writer fsck --full --no-progress` succeeds after recovery, with its valid branch/ref and preserved reconciliation evidence.
`systemctl --user is-active dracon-sync.service` returns `active`, and the installed `dracon-sync --version` matches the newly released source tag.

## Audit history

- 2026-08-05T05:54:18.200Z — disapproved — `opencode-go/deepseek-v4-flash`
