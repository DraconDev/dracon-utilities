# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on

## Objective

> Fix audit finding: LOW: evaluate_link compares raw (non-canonicalized) paths when both sides fail canonicalize, so equivalent forms like `~/a/../b` vs `~/b` report link_target_mismatch for an in-sync link (dracon-system/src/links.rs:66-72) —

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").
