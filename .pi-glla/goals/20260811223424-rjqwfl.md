# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on
**File**: `20260811223424-rjqwfl.md`
**Pause reason**: auditor disapproved
**Agent suggests**: Inspect auditor feedback and fix the actual gap before calling complete_goal again

## Objective

> Fix audit finding: MEDIUM: V1 fallback CFB decrypt (`allow_v1_fallback`) is unauthenticated — the only filter is "first 20 bytes ASCII"; a wrong-key decrypt whose CFB output passes the heuristic is returned as silent garbage plaintext (whole-file secret corruption); error path leaks first 20 bytes of ciphertext to stderr (crypto.rs:184-201) —

## Completion summary

The legacy V1 CFB fallback is hardened. `decrypt_git_seal` now rejects decrypted data unless the entire buffer is valid UTF-8 text and its initial bytes pass the existing text gate, preventing ASCII-prefix garbage from being returned as plaintext. Decryption failure reporting no longer includes ciphertext bytes, so the error path does not leak secret material.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").

## Audit history

- 2026-08-12T05:07:18.894Z — disapproved — `openai-codex/gpt-5.6-luna`
