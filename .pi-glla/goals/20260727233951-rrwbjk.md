# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on
**File**: `20260727233951-rrwbjk.md`

## Objective

> **Auto-mirror retry-softening fix in `dracon-sync`'s concern path.**

The 73-minute browser-extensions-shared stall this session was caused by the daemon's `repair-concerns → create_private_remote` path being too eager for transient origin-unreachable cases. Fix:
- In `dracon-sync/src/concerns.rs` (or wherever the "NO_ORIGIN,EMPTY_REPO" detect lives) — retry `git ls-remote origin HEAD` 3× with 5s delay before declaring origin gone.
- Only call `create_private_remote` if origin has been gone for > 15 minutes AND the repo was never pushed (no `origin/main` ever existed on this checkout).
- Log a clear distinction: "transient ssh hiccup — will retry" vs "origin really gone, creating offline mirror".

**Verification contract:**
- New unit test `concerns_retry_softening` reproducing the original behavior: simulate 2 transient `ls-remote` failures + 1 success → daemon logs "transient hiccup, will retry", does NOT create a mirror.
- Test `concerns_retry_softening_really_gone`: simulate 5+ persistent `ls-remote` failures → daemon logs "origin gone, creating mirror", DOES create it.
- `cargo test --workspace --locked` ≥ 1038 + 2 new tests pass.
- `cargo clippy --workspace --locked --all-targets -- -D warnings` exit 0.
- Release-notes entry added to next `v0.113.x` CHANGELOG.

**Hard out-of-scope:** rewriting the whole concern path, fixing other MEDIUM/LOW audit findings.
</item>
