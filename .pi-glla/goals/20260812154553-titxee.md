# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on
**File**: `20260812154553-titxee.md`
**Pause reason**: auditor disapproved
**Agent suggests**: Inspect auditor feedback and fix the actual gap before calling complete_goal again

## Objective

> Fix audit finding: MEDIUM: `gather_all_recipients` trusts ANY `*.pub`/`*.key` under `.dracon/data/keys` and `.git/arcane/keys` with no `owner_*`/content validation (unlike the publish path main.rs:874-880) — any contributor who can push a file adds a recipient silently included in every future encryption (decryption access to all subsequent secrets, no review) (crypto.rs:104-133) —

## Completion summary

Implemented and committed the repository-recipient authorization fix. `gather_all_recipients` now treats `.dracon/data/keys` and `.git/arcane/keys` as contributor-controlled inputs: only canonical public-key candidates with exactly one valid age recipient matching a local identity or HOME trust anchor are accepted; HOME key directories remain intentionally permissive. Documentation and regression coverage were updated, and the finding ledger is checked.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").

## Audit history

- 2026-08-12T21:25:46.613Z — disapproved — `openai-codex/gpt-5.6-luna`
- 2026-08-13T07:01:31.899Z — disapproved — `openai-codex/gpt-5.6-luna`
