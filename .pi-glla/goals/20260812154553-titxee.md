# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260812154553-titxee.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msqlcezs-5lwaaj`

## Objective

> Fix audit finding: MEDIUM: `gather_all_recipients` trusts ANY `*.pub`/`*.key` under `.dracon/data/keys` and `.git/arcane/keys` with no `owner_*`/content validation (unlike the publish path main.rs:874-880) — any contributor who can push a file adds a recipient silently included in every future encryption (decryption access to all subsequent secrets, no review) (crypto.rs:104-133) —

## Completion summary

The repository-recipient trust issue is fixed. `gather_all_recipients` now treats contributor-pushable repository key directories as strict trust boundaries: only canonical `owner_*.pub` files are considered, suspicious/oversized/secret-material files are rejected, and candidate lines must parse as real age x25519 recipients. Both `.dracon/data/keys` and legacy `.git/arcane/keys` use this gate. HOME keys remain intentionally operator-trusted for compatibility. The fix is committed and the audit ledger is checked.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").
