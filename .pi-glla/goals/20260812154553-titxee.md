# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260812154553-titxee.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msr9lbpu-pe04df`

## Objective

> Fix audit finding: MEDIUM: `gather_all_recipients` trusts ANY `*.pub`/`*.key` under `.dracon/data/keys` and `.git/arcane/keys` with no `owner_*`/content validation (unlike the publish path main.rs:874-880) — any contributor who can push a file adds a recipient silently included in every future encryption (decryption access to all subsequent secrets, no review) (crypto.rs:104-133) —

## Completion summary

Completed the repository recipient trust-boundary fix and hardened the dependent repository-key discovery paths. `gather_all_recipients` now accepts only canonical owner/master files matching local trust anchors, or noncanonical machine/team/direct recipients carrying authenticated proof bound to the exact filename, recipient, and repository key; HOME/repository overlap and symlink paths fail closed. Machine-only ARCANE_MACHINE_KEY loading now requires owner-DH proof, and arbitrary `.age` discovery is restricted. The finding ledger is checked with the final fix commit.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").

## Audit history

- 2026-08-12T21:25:46.613Z — disapproved — `openai-codex/gpt-5.6-luna`
- 2026-08-13T07:01:31.899Z — disapproved — `openai-codex/gpt-5.6-luna`
