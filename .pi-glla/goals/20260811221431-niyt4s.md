# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260811221431-niyt4s.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msp85g5g-gfwe3i`

## Objective

> Fix audit finding: MEDIUM: pre-push hook regex requires quotes (`password\s*=\s*["'][^"']+`, main.rs:2645) and the inline scanner's "Generic Secret (Unquoted)" is commented out (scanner.rs:352-355) / "Password Variable (Unquoted)" requires `=` immediately after the name (365-367) → `password = hunter2` in a protected text file (secrets/app.yaml) commits plaintext AND passes the pre-push defense-in-depth; pre-push also never scans binary additions (`git diff` emits no `+` lines for binaries) —

## Completion summary

The plaintext pre-push defense gap is fixed. The hook now catches whitespace-padded unquoted password assignments and scans complete blobs for newly added files, including binary additions; the inline security scanner also recognizes whitespace-tolerant unquoted secret assignments. The tests exercise both blocking and clean paths without relying on vacuous self-matches.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").
