# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260811210833-eaqh4e.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msp5odgl-uen35o`

## Objective

> Fix audit finding: HIGH: SIGHUP reload restores only the legacy `reniced_pids` map then resets `runtime = GuardRuntimeState::default()` (main.rs:4115-4122) — v0.112.36 `memory_reniced_pids`/`oom_biased_pids`/`capped_pids` are dropped WITHOUT restore: oom-biased pids stay at oom_score_adj=250 permanently (next critical pass reads 250 → `oom_bias_target(250)` → None → never re-tracked), reniced pids stay at nice 5-15, capped pids linger in throttled units; daemon shutdown/restart has no restore hook either (main.rs:917-922) —

## Completion summary

The HIGH SIGHUP/shutdown restoration finding is fixed and verified. The guard now builds one complete restoration plan for every reversible limiter, restores all active adjustments before SIGHUP runtime replacement and on graceful shutdown, and preserves tracking when process identity, cgroup state, or cleanup commands are indeterminate or fail.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").
