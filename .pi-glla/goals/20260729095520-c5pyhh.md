# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on
**File**: `20260729095520-c5pyhh.md`

## Objective

> Repos table v2 + exclude-aware dirty semantics (operator feedback 2026-07-29): (1) daemon status/dirty detection classifies porcelain entries against auto_commit_exclude_patterns — repos whose only dirty files are excluded show ACTIVITY as `🟢 synced Nm · N excl` and can NEVER escalate to WARN; WARN requires daemon-committable dirt stalling beyond the normal commit window; (2) parent-repo dirty detection ignores submodule-worktree-only dirtiness (--ignore-submodules=dirty semantics) while still counting gitlink SHA drift, so dracon-platform stops inheriting junk-runner's excluded-file WARN; (3) drop the USED column (operator: duplicates ACTIVITY); (4) split COMMITS into three separate columns (1H | 6H | 24H) reusing the freed width; (5) legend updated to match (remove USED line, add `N excl` + new column headers)

## Verification contract

junk-runner and dracon-platform no longer WARN on excluded-only dirt (verified via live `dracon-sync repos`); table has no USED column and three commit-window columns; legend matches the shipped columns; existing table snapshot/unit tests updated; cargo test --workspace --locked + cargo clippy --workspace --locked -- -D warnings green; shipped as a release per the full contract (release.sh).
