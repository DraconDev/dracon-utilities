# Goal

**Status**: paused
**Policy**: list
**Auto-continue**: on
**File**: `20260729061455-x0mwth.md`
**Pause reason**: send-retry storm: 5m of 50ms re-arms — the session never went idle for the continuation
**Agent suggests**: The session never went idle for the send (wedged queue or permanently busy). Restart pi, then /goal resume.

## Objective

> CAG Option A: execute the capture-anime-girls history rewrite per docs/design/cag-github-push-block-corrected-2026-07-28.md — bundle backup first, then filter-repo strip .pi/, .pi-tmp/, docs/audit*/ from all history, force-push main to github+gitlab+codeberg (DRACON_ALLOW_REWRITE=1 per-invocation; unprotect+re-protect gitlab main around the push)

## Verification contract

CAG .git dir < 1 GiB after gc, all three remotes' main tips equal the rewritten local main, github push succeeds (no 2 GiB rejection), and `dracon-sync repos` shows CAG no longer ❌ CONCERN (game art intact: static/audio MP3s + sprites still reachable on main).
