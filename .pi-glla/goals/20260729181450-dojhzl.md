# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260729181450-dojhzl.md`

## Objective

> Post-completion regression scan (reviewer follow-up after janitor spot-check close-out): sweep the workspace + live fleet for anything the last two releases (v0.113.12 legend footer, v0.113.13 table v2 + exclusion-aware dirty semantics) may have regressed. Checks: (1) cargo test --workspace --locked + clippy --workspace --locked -- -D warnings green at HEAD; (2) live render sanity across tiers — COLUMNS=200 rich, COLUMNS=100 compact, repos --json parses, `dracon-sync repos junk-runner` detail renders; legend appears at wide widths only; (3) the exclude-aware classifier didn't over-suppress — a repo with real committable dirt still shows ⏳ dirty; (4) daemon health: systemctl active, no new ERROR/panic bursts in journal since the 12:07 UTC restart, no 🟣 PENDING stuck >15min; (5) session residue committed

## Verification contract

all five checks pass (or failures have committed fixes + re-run evidence) and the outcome is committed as a .pi-glla note in dracon-utilities.
