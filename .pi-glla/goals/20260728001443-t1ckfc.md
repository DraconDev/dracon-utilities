# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260728001443-t1ckfc.md`

## Objective

> **LOW-hygiene clippy --all-targets: fix the 17 pre-existing test-code lints in `dracon-sync`.**

From the parked goal id `20260726235933-0ik9uq`. Apply per-lint fixes or batch `#[allow(clippy::lint_name)]` per module. Lints include `useless_vec!`, `bool_assert_comparison`, and 15 others (final list from `cargo clippy --workspace --locked --all-targets -- -D warnings 2>&1 | grep -cE "^error"`).

**Verification contract:**
- `cargo clippy --workspace --locked --all-targets -- -D warnings` exits 0.
- `cargo test --workspace --locked` still passes (≥ 1038 tests).
- No `--all-targets` regression: re-run the command against `HEAD` and prove clean.
- Per-lint summary: N fixed vs M allowed (with a one-line justification for each allow).
- CHANGELOG entry under `[Unreleased] — LOW-hygiene clippy --all-targets cleanup`.

**Hard out-of-scope:** fixing non-test-code lints (production code), banning lints globally, breaking changes to test setup.
</item>
