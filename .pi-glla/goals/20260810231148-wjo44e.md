# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260810231148-wjo44e.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msnuqfht-hcrcrl`

## Objective

> FIX MEDIUM (warden): gather_all_recipients trusts any *.pub/*.key without owner_* validation — silent recipient addition to all future encryptions (crypto.rs:104-133)

## Completion summary

FIX MEDIUM (warden) shipped: gather_all_recipients no longer trusts any *.pub/*.key file as a recipient source. The old load_public_recipients_from_dir accepted every .pub/.key under ~/.dracon/data/keys and the repo dirs (.dracon/data/keys, .git/arcane/keys) and parsed every line — so any contributor who could push a file added its holder to ALL future encryptions silently. Fix (dracon-warden, commits d44c2ef + b259b33, all mirrors at HEAD b259b33): repo key dirs (the contributor-pushable surface) now honor ONLY canonical owner_*.pub mesh files (mirrors is_owner_pubkey_filename from main.rs) — a pushed evil.pub with a valid age recipient is refused with a warn-once eprintln; content validation was added everywhere mirroring validate_owner_age_pubkey_bytes (files containing AGE-SECRET-KEY- material or >256 bytes are refused wholesale, per-line x25519::Recipient parse retained); the operator's HOME key dir stays permissive (its own trust domain — micro2_*.pub/master.pub remain honored, avoiding lockout) but flags non-canonical names once; the home lookup now uses get_home() so mock_home is honored (testable). One new test (test_gather_all_recipients_validates_recipient_files) proves: owner_*.pub mesh key included, non-owner evil.pub excluded, secret-material file excluded, home-dir non-owner key still included. Gates green: dracon-security 52 lib tests + all suites, warden bin 101 tests, workspace --locked release build, clippy -D warnings clean. Installed ~/.local/bin/dracon-warden refreshed. Ledger line flipped [x] — fixed in b259b33; parent repo synced to all 3 mirrors.
