# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260811203241-3fufxm.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msp4r0dt-d0e7cy`

## Objective

> FIX LOW (warden): create_team writes team key with default umask + no overwrite guard; accept_team_invite does it right (team.rs:88 vs 176-190)

## Completion summary

Fixed the team-key creation security inconsistency. `create_team` now uses the same exclusive private-file creation path as invite acceptance: on Unix the encrypted key is created with mode 0600 and `create_new`, so default umask permissions are never exposed and repeated or concurrent creation cannot overwrite an existing key. The shared helper also hardens invite acceptance consistently.
