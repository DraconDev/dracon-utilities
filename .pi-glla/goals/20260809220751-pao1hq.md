# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260809220751-pao1hq.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msmd11sx-3vxzim`

## Objective

> Fix audit finding: MEDIUM: whole-file secret-tag detection accepts at most ONE trailing `\n` (single `strip_suffix(b"\n")`), so a whole-file-encrypted BINARY secret whose blob gained CRLF or 2+ trailing newlines falls into the UTF-8-lossy smudge path (U+FFFD corruption of DER/SQLite/.kdbx, re-encrypted into history by next clean) or double-encryption on clean (dracon-warden/src/security/src/lib.rs:856 and 833) —

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").
