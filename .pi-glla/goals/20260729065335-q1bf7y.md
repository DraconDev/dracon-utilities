# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on

## Objective

> dracon-sync release.sh hardening: derive the github remote from `git config --get-regexp '^remote\..*\.url$'` (URL contains github.com) instead of the hardcoded name `github`, and make the CHANGELOG-close step idempotent (re-running on an already-closed version must NOT duplicate the `## [x.y.z]` header)

## Verification contract

`release.sh <next-version> --dry-run` passes the remote-resolution step on this repo (where github is named `origin`), a re-run against an already-closed CHANGELOG leaves it byte-identical, and the script still fails loudly when no github remote exists.
