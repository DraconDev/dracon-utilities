# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260728111602-xwwe9z.md`

## Objective

> Surface github-pack-too-large as CONCERN (with auto-repair no-op) — daemon's silent skip becomes visible in `dracon-sync repos`, auto-repair doesn't waste cycles. Investigation: report.rs:3068 `let warn = !concern && real_is_dirty` ignores `pack_too_large`.0. Fix: when `pack_too_large.0`, set concern=true. Auto-repair path checks PACK_SIZE_WARNING and no-ops

## Verification contract

repos shows CAG as ❌ CONCERN, +1 unit test, +1 design doc, CHANGELOG entry, deployed binary restarts clean, no auto-repair noise in journalctl.
