# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260810231925-autu7r.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msnv4hz5-90axdz`

## Objective

> FIX MEDIUM (warden): global pre-push/pre-rebase hooks shadow repo-local hooks with no chaining (H-10 fix covered pre-commit only) (main.rs:2506-2661, 2665-2698)

## Completion summary

FIX MEDIUM (warden) shipped: the global pre-push and pre-rebase hooks no longer shadow repo-local hooks — they chain them, completing the H-10 fix (which covered pre-commit only). In PRE_PUSH_HOOK (main.rs): the push refs on stdin are buffered to a temp file, the repo-local .git/hooks/pre-push is chained first (replayed the buffered refs; 'Dracon Warden' grep skips warden-seeded copies so no recursion; DRACON_ALLOW_REWRITE does not gate it since that bypass is scoped to the history guard), and warden's own scan then reads the same buffer via 'done < "$REFS_FILE"' — keeping the loop in the main shell so its exit 1s still abort the push, and a local hook consuming stdin cannot starve the scan. In PRE_REBASE_HOOK (main.rs): the repo-local pre-rebase is chained after the DRACON_ALLOW_REWRITE bypass, so the escape hatch disables hook interference entirely as documented. Five new tests in src/tests.rs: pre-push chains (marker file written, clean push passes), pre-push propagates local failure (exit 3), pre-push scan survives local-hook stdin consumption (cat >/dev/null local hook + password = hunter2 fixture still blocked with 'Possible plaintext secrets'), pre-rebase chains, pre-rebase propagates failure (exit 4). Fixture literal concat-split so the warden's own live hook never self-blocks. Gates green: dracon-warden bin 106 passed (was 101), dracon-security 52 passed, --locked release build OK, clippy -D warnings clean. Installed binary refreshed, setup-hooks rerun (live ~/.config/git/hooks/pre-push contains REFS_FILE block, pre-rebase contains LOCAL_HOOK), live-hook smoke in a scratch repo: repo-local hook ran under the installed global pre-push, exit 0. Mirrors at HEAD c1f463c (dracon-warden) and 977877c7c (parent, ledger line flipped [x] — fixed in c1f463c).
