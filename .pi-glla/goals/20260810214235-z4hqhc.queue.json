{"schema":1,"type":"queue-item","id":"20260810214235-z4hqhc","objective":"Fix audit finding: HIGH: path_is_protected() multi-component `**/` glob branch is dead code — `path_str.contains(\"audit/**\")` requires a literal `*` inside the path, so `**/secrets/**` never matches any real path; such files still get `filter=dracon` in .gitattributes (git's own globset DOES match `**/`), but the clean gate sees them as unprotected and passes them through UNENCRYPTED (dracon-warden/src/security/src/modules/filter.rs:98-105) —","verificationContract":"the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked (\"- [x] … — fixed in <commit>\").","addedAt":"2026-08-10T21:42:35.617Z"}