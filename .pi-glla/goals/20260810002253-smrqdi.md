# Goal

**Status**: paused
**Policy**: list
**Auto-continue**: on
**File**: `20260810002253-smrqdi.md`
**Pause reason**: main model recovery — retrying in 36m (main model unknown: Stream ended without finish_reason — All three scenarios correct. Fix a cosmetic double-space in the die_pre message:)
**Agent suggests**: The provider/quota wall is being retried automatically; configured backup models are tried in order. /list resume retries immediately; /list cancel stops it.

## Objective

> Fix audit finding: LOW: `--abort` help text claims it "Refuses to run if the working tree was already dirty at start" — the abort path performs no such check (dracon-sync/scripts/release.sh:26-28 vs ~:130) —

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").
