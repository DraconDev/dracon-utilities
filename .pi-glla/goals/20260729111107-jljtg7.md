# Goal

**Status**: paused
**Policy**: list
**Auto-continue**: on
**File**: `20260729111107-jljtg7.md`
**Pause reason**: Time-gated by design: the janitor (auto_prune_stale_backup_branches, v0.113.10) needs ≥24h of daemon uptime before the spot-check is meaningful. It is currently 2026-07-29 12:51 UTC; the daemon was also restarted at 11:07 UTC for the v0.113.13 install. Gate lifts ~06:40 UTC 2026-07-30. Interim evidence healthy: 0 janitor (🧹) log lines, no ~/dracon/backups/auto-prune/ dir, zero backup/* branches fleet-wide. Running the check now would only re-confirm silence against a 6h-old enablement.
**Agent suggests**: When the pause lifts, run the three contract checks: (1) journalctl --user -u dracon-sync.service --since 2026-07-29T06:39 | grep 🧹 (any deletion must have a matching bundle in ~/dracon/backups/auto-prune/ + logged tip); (2) ls ~/dracon/backups/auto-prune/ (expect empty or fully-explained bundles); (3) fleet-wide sweep for backup/* branches. Then commit the outcome as a .pi-glla note in dracon-utilities and complete_goal.

## Objective

> Janitor production spot-check (v0.113.10 auto_prune_stale_backup_branches, enabled fleet-wide 2026-07-29 ~06:39 UTC): after ≥24h of daemon uptime (i.e. any time after 06:40 UTC 2026-07-30),

## Verification contract

(1) journalctl --user -u dracon-sync.service --since 2026-07-29T06:39 | grep 🧹 — expect zero UNEXPECTED deletions; any deletion must have a matching bundle in ~/dracon/backups/auto-prune/ and a logged tip; (2) ls ~/dracon/backups/auto-prune/ — expect empty or fully-explained bundles; (3) sweep for backup/* branches fleet-wide to confirm no candidates were missed. Done when: the three checks are run and the outcome committed as a .pi-glla note in dracon-utilities. Interim check at ~1h uptime was healthy: 0 janitor log lines, no auto-prune dir, no backup/* branches fleet-wide.
