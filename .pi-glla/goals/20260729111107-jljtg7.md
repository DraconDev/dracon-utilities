# Goal

**Status**: paused
**Policy**: list
**Auto-continue**: on
**File**: `20260729111107-jljtg7.md`
**Pause reason**: Time-gated: requires ≥24h of daemon uptime with the janitor enabled (enabled 2026-07-29 ~06:39 UTC). It is currently ~11:50 UTC — ~5h in. The full check becomes meaningful after 06:40 UTC 2026-07-30. Interim evidence healthy: 0 janitor log lines, no ~/dracon/backups/auto-prune/ dir, zero backup/* branches fleet-wide. (Re-queued after being silently resolved when the table-v2 item activated — the table work is now complete and shipped as v0.113.13.)
**Agent suggests**: Re-activate after 06:40 UTC 2026-07-30 and run the three checks from the item text: journalctl 🧹 grep (expect no unexpected deletions; every deletion needs a matching bundle + logged tip), ls ~/dracon/backups/auto-prune/ (expect empty or explained), fleet sweep for backup/* branches. Commit the outcome as a .pi-glla note.

## Objective

> Janitor production spot-check (v0.113.10 auto_prune_stale_backup_branches, enabled fleet-wide 2026-07-29 ~06:39 UTC): after ≥24h of daemon uptime (i.e. any time after 06:40 UTC 2026-07-30),

## Verification contract

(1) journalctl --user -u dracon-sync.service --since 2026-07-29T06:39 | grep 🧹 — expect zero UNEXPECTED deletions; any deletion must have a matching bundle in ~/dracon/backups/auto-prune/ and a logged tip; (2) ls ~/dracon/backups/auto-prune/ — expect empty or fully-explained bundles; (3) sweep for backup/* branches fleet-wide to confirm no candidates were missed. Done when: the three checks are run and the outcome committed as a .pi-glla note in dracon-utilities. Interim check at ~1h uptime was healthy: 0 janitor log lines, no auto-prune dir, no backup/* branches fleet-wide.
