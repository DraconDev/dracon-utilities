# Goal

**Status**: auditing
**Policy**: list
**Auto-continue**: on
**File**: `20260811205419-8sopcw.md`
**Completion audit**: running
**Completion audit attempt**: `audit-msp546zd-va2j7v`

## Objective

> Fix audit finding: HIGH: parent `scripts/release.sh:71` hardcodes `REMOTE=github` but this repo's GitHub remote is `origin` (verified `git remote -v`) — step 6b/6c (`git push "$REMOTE" HEAD:main`, `git push "$REMOTE" "$TAG"`, ~lines 449/465) fail out of the box with "fatal: 'github' does not appear to be a git repository"; the v0.113.11 resolve-github-remote.sh treatment was ported to sync and warden but never to the parent script; help text matches the (wrong) default so there is no hint —

## Completion summary

The parent release-remote finding is fixed and verified on the current branch. `scripts/release.sh` now derives the GitHub push remote from its URL via the standalone resolver, accepts and validates explicit `--remote` overrides, and uses the resolved name for both commit and tag pushes; this checkout correctly resolves `origin` instead of assuming `github`.

## Verification contract

the fix is committed on the current branch with the repo's configured identity, and this finding's box in .pi-glla/audit-loop/findings.md is checked ("- [x] … — fixed in <commit>").
