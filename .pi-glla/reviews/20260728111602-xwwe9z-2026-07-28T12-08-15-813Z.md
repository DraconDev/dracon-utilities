# Review — 20260728111602-xwwe9z

**Kind**: list · **At**: 2026-07-28T12:08:15.813Z · **Mode**: default

## Summary

Completed: Surface github-pack-too-large as CONCERN (with auto-repair no-op) — daemon's silent skip becomes visible in `dracon-sync repos`, auto-repair doesn't waste cycles. Investigation: report.rs:3068 `let warn = !concern && real_is_dirty` ignores `pack_too_large`.0. Fix: when `pack_too_large.0`, set concer

**Cascade step**: queue-leftovers

## Findings (10)

## Bug-class (enqueued to /list, no Confirm)

- The actual file content shows that line 3068 in the original refers to   — and the executor's claim was correct about that bug. The fix above correctly routes through  . Now let me check the auto-repa _(audit)_
- Wait — I see the issue. The   call on line 6415-6422 produces flags but **does NOT include PACK_SIZE_WARNING**. The PACK_SIZE_WARNING flag is added separately on line 3157 in the row-construction code _(audit)_
- This is a real bug in the executor's work — the no-op guard is dead code in   because it checks   from  , which never adds PACK_SIZE_WARNING. The "concern is found" works (because of the line 6391-639 _(audit)_
- For CAG specifically, no handlers match so   by accident. But for a hypothetical repo that ALSO has a CONCERN and ALSO has pack_too_large, the auto-repair would attempt handlers. So the guard is broke _(audit)_
- Good. The auto-repair loop runs after each sync pass. Now, given the bug I identified (PACK_SIZE_WARNING is not in   from  ), the no-op guard at line 6433 is dead code. But empirically, the live   vie _(audit)_
- But the **claimed mechanism** is broken: the no-op guard doesn't fire; the no-op happens by coincidence. If a CAG repo later also had a real concern (e.g.,  ), the dead guard wouldn't short-circuit an _(audit)_
- So the no-op guard DOES NOT fire for a stuck-push repo with pack_too_large. This is a real bug. But for the current state of CAG (no stuck-push), the outcome is correct anyway. _(audit)_
- This is a subtle bug that violates the executor's claim #7 about the no-op firing. However, for the actual user-facing contract — "auto-repair doesn't waste cycles" — empirically for CAG, no cycles ar _(audit)_
- OK so "resolved: concern cleared" is printed whenever the post-check   — which it would be for CAG (clean, no upstream issue). That's expected for a CONCERN that no longer applies. But wait — CAG's CO _(audit)_
- But wait — this might be fine because in a subsequent repos run, the row will still show CONCERN (since the row construction uses   to set the flag). So   here is misleading but not permanently broken _(audit)_




